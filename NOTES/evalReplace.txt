IF _rw in buildingName and is_listed==no
#upgrades to enhanced buildings. Should always be a priority
	ai_weight = { weight = @crucial_2 }

EVAL building.buildingName.find("biolab")!=-1
ai_weight = { weight = @crucial_2 }

IF _rw in buildingName and is_listed==no
#upgrades to enhanced buildings. Should always be a priority
	ai_weight = { weight = @crucial_2 }