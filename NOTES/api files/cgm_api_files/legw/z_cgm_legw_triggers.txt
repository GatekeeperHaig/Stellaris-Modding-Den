has_legw_planet_bonus_building = {
	OR = {
		has_building = pm2_tb_crashed
		has_building = pm2_tb_gate
		has_building = pm2_tb_gate_buffed
		has_building = pm2_tb_gate_closed
		has_building = pm2_tb_pre
		has_building = pm2_tb_rift
	}
}
had_legw_planet_bonus_building = {
	OR = {
		has_prev_building = pm2_tb_crashed
		has_prev_building = pm2_tb_gate
		has_prev_building = pm2_tb_gate_buffed
		has_prev_building = pm2_tb_gate_closed
		has_prev_building = pm2_tb_pre
		has_prev_building = pm2_tb_rift
	}
}
