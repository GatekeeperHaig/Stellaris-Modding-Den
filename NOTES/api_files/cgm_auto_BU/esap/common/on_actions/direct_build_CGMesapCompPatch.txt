on_building_complete = { events = { direct_build_CGMesapCompPatch.1 } }
on_building_replaced = { events = { direct_build_CGMesapCompPatch.1 } }
 #make sure downgraded buildings are build upgraded for some strange reason:
on_planet_transfer = { events = { direct_build_CGMesapCompPatch.3 } }
