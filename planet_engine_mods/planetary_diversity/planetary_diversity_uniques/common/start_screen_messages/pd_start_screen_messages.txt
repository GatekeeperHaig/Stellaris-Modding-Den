# Start Screen Messages
# Parameters:
#   trigger, a trigger with country which decides if this is a valid part. Empty will be treated as always=yes.
#   localization, a key to a localized string.
#	location, decides where this chunk of text is located in the description, cannot be negative.

### FIRST PARTS

# Floating World
part = {
	location = 0
	localization = "START_SCREEN_PART_ONE_FLOATING"
	
	trigger = {
		ideal_planet_class = pc_floating
		NOR = { 
			has_country_flag = custom_start_screen 
			has_authority = auth_hive_mind
			has_authority = auth_machine_intelligence
		}
	}
}

# Crystal World
part = {
	location = 0
	localization = "START_SCREEN_PART_ONE_CRYSTAL"
	
	trigger = {
		ideal_planet_class = pc_crystal
		NOR = { 
			has_country_flag = custom_start_screen 
			has_authority = auth_hive_mind
			has_authority = auth_machine_intelligence
		}
	}
}

# Graveyard World
part = {
	location = 0
	localization = "START_SCREEN_PART_ONE_GRAVEYARD"
	
	trigger = {
		ideal_planet_class = pc_graveyard
		NOR = { 
			has_country_flag = custom_start_screen 
			has_authority = auth_hive_mind
			has_authority = auth_machine_intelligence
		}
	}
}

# Archive World
part = {
	location = 0
	localization = "START_SCREEN_ARCHIVE_ORIGIN_ORG"
	
	trigger = {
		ideal_planet_class = pc_archive
		has_country_flag = custom_start_screen 
		NOR = { 
			has_authority = auth_hive_mind
			has_authority = auth_machine_intelligence
		}
	}
}

part = {
	location = 0
	localization = "START_SCREEN_ARCHIVE_ORIGIN_ROB"
	
	trigger = {
		has_country_flag = custom_start_screen 
		has_authority = auth_machine_intelligence
		NOT = { 
			has_authority = auth_hive_mind
			is_regular_empire = yes
		}
	}
}

# Technoorganic World
part = {
	location = 0
	localization = "START_SCREEN_PART_ONE_TECHNOORGANIC"
	
	trigger = {
		ideal_planet_class = pc_technoorganic
		NOR = { 
			has_country_flag = custom_start_screen 
			has_authority = auth_hive_mind
			has_authority = auth_machine_intelligence
		}
	}
}
part = {
	location = 0
	localization = "START_SCREEN_TECHNO_ORIGIN"
	
	trigger = {
		ideal_planet_class = pc_technoorganic
		has_country_flag = custom_start_screen 
		NOR = { 
			has_authority = auth_machine_intelligence
		}
	}
}

# Crucible World
part = {
	location = 0
	localization = "START_SCREEN_CRUCIBLE_ORIGIN"
	
	trigger = {
		has_country_flag = custom_start_screen 
		has_origin = origin_crucible_world
		NOR = { 
			has_authority = auth_hive_mind
			has_authority = auth_machine_intelligence
		}
	}
}