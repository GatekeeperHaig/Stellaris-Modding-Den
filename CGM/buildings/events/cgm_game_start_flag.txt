namespace = cgm_gamestart_flag
event = {
	id = cgm_gamestart_flag.1
	hide_window = yes
	fire_only_once = yes
	is_triggered_only = yes
  trigger = { NOT = { has_global_flag = cgm_game_start } }
	immediate = {
		set_global_flag = cgm_game_start
		set_timed_global_flag = { flag = cgm_game_start_timed days = 1 }
	}
}
