namespace = ex_combat_flag

country_event = { ###this event fire once, setting a flag 
	id = ex_combat_flag.1
	hide_window = yes	
	fire_only_once = yes
	
	trigger = {	NOT = { has_global_flag = ex_combat_active } }
	
	immediate = { set_global_flag = ex_combat_active }
}