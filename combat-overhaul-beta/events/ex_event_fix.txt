name_space = ex_event_fix

country_event = { ###this event is made to balance out the Automated Dreadnought by moving its bonus modifiers from the ship size to a ship modifier. 
	id = ex_event_fix.1
	hide_window = yes	
	fire_only_once = yes
	
	trigger = {	is_country_type = guardian_dreadnought }
	
	immediate = {
		every_owned_ship = { add_modifier = ex_automated_dreadnought_buff days = -1 }
	}
}