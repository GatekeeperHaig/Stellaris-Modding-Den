#This file was created by script!
#Instead of editing it, you should change the origin files or the script and rerun the script!
#Python files that can be directly used for a rerun (storing all parameters from the last run) should be in the main directory
has_building_enhanced_betharian_power_plant = {
	
	OR = {
		has_building = building_enhanced_betharian_power_plant
		has_building = building_enhanced_betharian_power_plant_direct_build
	}
}
has_building_fe_xeno_zoo = {
	
	OR = {
		has_building = building_fe_xeno_zoo
		has_building = building_fe_xeno_zoo_direct_build
	}
}
