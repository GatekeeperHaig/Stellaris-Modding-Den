
event = {
	id = ex_combat_game_start.2
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		every_country = {
			if = { #torpedo, this modifier dictates how likely this country is to use Torpedo segments if it has the required techs.
				limit = { 
					is_country_type = default
				}				
				random_list = { # you can use any name for the flag
					30 = { set_country_flag = torpedo_light_pref modifier = { factor = 0.5 is_high_aggression_personality = yes } modifier = { factor = 0.5 is_low_aggression_personality = yes } }
					20 = { set_country_flag = torpedo_medium_pref modifier = { factor = 0.25 is_high_aggression_personality = yes } modifier = { factor = 0.25 is_low_aggression_personality = yes } }
					10  = { set_country_flag = torpedo_heavy_pref modifier = { factor = 0.125 is_high_aggression_personality = yes } modifier = { factor = 0.125 is_low_aggression_personality = yes } }
					40 = { } # this mean no flag will be added
				}	
			}	# there are two options here - either do a bunch of other if fields, or use else = { }
			if = { #PD, this modifier dictates how likely this country is to use PD segments if it has the required techs.
				limit = { 
					is_country_type = default
				}				
				random_list = { # you can use any name for the flag
					30 = { set_country_flag = pd_light_pref modifier = { factor = 0.5 is_high_aggression_personality = yes } modifier = { factor = 0.5 is_low_aggression_personality = yes } }
					20 = { set_country_flag = pd_medium_pref modifier = { factor = 0.25 is_high_aggression_personality = yes } modifier = { factor = 0.25 is_low_aggression_personality = yes } }
					10  = { set_country_flag = pd_heavy_pref modifier = { factor = 0.125 is_high_aggression_personality = yes } modifier = { factor = 0.125 is_low_aggression_personality = yes } }
					40 = { } # this mean no flag will be added
				}	
			}	# there are two options here - either do a bunch of other if fields, or use else = { }
			if = { #strike craft, this modifier dictates how likely this country is to use hangar segments if it has the required techs.
				limit = { 
					is_country_type = default
				}				
				random_list = { # you can use any name for the flag
					30 = { set_country_flag = strike_craft_light_pref modifier = { factor = 0.5 is_high_aggression_personality = yes } modifier = { factor = 0.5 is_low_aggression_personality = yes } }
					20 = { set_country_flag = strike_craft_medium_pref = { factor = 0.25 is_high_aggression_personality = yes } modifier = { factor = 0.25 is_low_aggression_personality = yes } }
					10  = { set_country_flag = strike_craft_heavy_pref modifier = { factor = 0.125 is_high_aggression_personality = yes } modifier = { factor = 0.125 is_low_aggression_personality = yes } }
					40 = { } # this mean no flag will be added
				}	
			}	# there are two options here - either do a bunch of other if fields, or use else = { }
			
			if = { #capital weapons, this modifier dictates how likely this country is to use xl segments if it has the required techs.				 
				limit = { 
					is_country_type = default
				}				
				random_list = { # you can use any name for the flag
					30 = { set_country_flag = capital_light_pref modifier = { factor = 0.5 is_high_aggression_personality = yes } modifier = { factor = 0.5 is_low_aggression_personality = yes } }
					20 = { set_country_flag = capital_medium_pref modifier = { factor = 0.25 is_high_aggression_personality = yes } modifier = { factor = 0.25 is_low_aggression_personality = yes } }
					10  = { set_country_flag = capital_heavy_pref modifier = { factor = 0.125 is_high_aggression_personality = yes } modifier = { factor = 0.125 is_low_aggression_personality = yes } }
					40 = { } # this mean no flag will be added
				}	
			} 
			if = { #aggressive defensive personality, this modifier dictates how likely this country is to prefer aggressive or defensive combat behavior.
				limit = { is_country_type = default	}				
				random_list = { # you can use any name for the flag
					50 = { set_country_flag = aggressive_behavior_pref }
					50 = { set_country_flag = defensive_behavior_pref }
				}	
			}			
		}
	}
}