check_planet_bonus_buildings_eac = {
	if = {
		limit = {
			NAND = {
				exists = owner
				owner = { has_authority = auth_machine_intelligence }
			}
		}
		#UNIQUE
		if = {
			limit = { has_building = eac_building_food_packaging_plant_1 }
			change_variable = { which = food_mult_planet_building value = 0.10 }
		}
		if = {
			limit = { has_building = eac_building_food_packaging_plant_2 }
			change_variable = { which = food_mult_planet_building value = 0.20 }
		}
	}
	#UNIQUE
	if = {
		limit = { has_building = eac_building_anomaly_research_facility }
		change_variable = { which = physics_research_mult_planet_building value = 0.05 }
	}
	if = {
		limit = { has_building = eac_building_alchemists_forge }
		change_variable = { which = engineering_research_mult_planet_building value = 0.05 }
	}
	if = {
		limit = {
			NAND = {
				exists = owner
				owner = {
					NOT = { has_authority = auth_machine_intelligence has_government = gov_machine_servitor }
				}
			}
		}
		#UNIQUE
		if = {
			limit = { has_building = eac_building_paste_processing_plant }
			change_variable = { which = food_mult_planet_building value = 0.10 }
		}
	}
}
