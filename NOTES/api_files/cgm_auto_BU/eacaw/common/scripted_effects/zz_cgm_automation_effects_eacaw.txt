 # This file was created by script!
 # Instead of editing it, you should change the python script.
 # Changes to the file will be overwritten the next time the script is run.
add_physics_research_building_eacaw = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = {
		add_building_construction = eac_building_planetary_shield_generator
		if = {
			limit = {
				OR = { has_building_construction = yes has_building = yes }
			}
			owner = { set_country_flag = cgm_auto_built }
		}
		else = { add_building_construction = eac_building_planetary_proton_cannon }
	}
}
add_engineering_research_building_eacaw = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = {
		add_building_construction = eac_building_planetary_rail_scattergun
		if = {
			limit = {
				OR = { has_building_construction = yes has_building = yes }
			}
			owner = { set_country_flag = cgm_auto_built }
		}
		else = {
			add_building_construction = eac_building_planetary_coil_scattergun
			if = {
				limit = {
					OR = { has_building_construction = yes has_building = yes }
				}
				owner = { set_country_flag = cgm_auto_built }
			}
			else = { add_building_construction = eac_building_planetary_shield_generator }
		}
	}
}
