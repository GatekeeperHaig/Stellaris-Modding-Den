has_unique_resource = {
	OR = {
		has_resource = { type = sr_ex_unique_1 amount > 0 }			
		has_resource = { type = sr_ex_unique_2 amount > 0 }						
		has_resource = { type = sr_ex_unique_3 amount > 0 }			
		has_resource = { type = sr_ex_unique_4 amount > 0 }						
		has_resource = { type = sr_ex_unique_5 amount > 0 }			
		has_resource = { type = sr_ex_unique_6 amount > 0 }							
	}
}	

has_planetary_strategic_resource = { 
	OR = {
		has_resource = { type = sr_ex_unique_1 amount > 0 }			
		has_resource = { type = sr_ex_unique_2 amount > 0 }						
		has_resource = { type = sr_ex_unique_3 amount > 0 }			
		has_resource = { type = sr_ex_unique_4 amount > 0 }						
		has_resource = { type = sr_ex_unique_5 amount > 0 }			
		has_resource = { type = sr_ex_unique_6 amount > 0 }							
		has_resource = { type = sr_rare_metals amount > 0 }
		has_resource = { type = sr_chromium amount > 0 }
		has_resource = { type = sr_zinorine amount > 0 }
		has_resource = { type = sr_lithium amount > 0 }
		has_resource = { type = sr_hivaron_stone amount > 0 }
		has_resource = { type = sr_archeological_remains amount > 0 }
		has_resource = { type = sr_ex_pitharan amount > 0 }
		has_resource = { type = sr_ex_engos amount > 0 }
		has_resource = { type = sr_ex_satramene amount > 0 }
		has_resource = { type = sr_betharian amount > 0	}
		has_resource = { type = sr_alien_pets amount > 0 }
	}
}	

has_orbital_strategic_resource = {
	OR = { 
		has_resource = { type = sr_thullium amount > 0 }
		has_resource = { type = sr_smartmetal amount > 0 }
		has_resource = { type = sr_sorium_3 amount > 0 }
		has_resource = { type = sr_magnetite amount > 0 }
		has_resource = { type = sr_xerodine amount > 0 }
		has_resource = { type = sr_hyperfuels amount > 0 }
		has_resource = { type = sr_scritch amount > 0 }
		has_resource = { type = sr_precursor_nanites amount > 0 }
		has_resource = { type = sr_prismatic_crystals amount > 0 }
		has_resource = { type = sr_hypder_diamond amount > 0 }
		has_resource = { type = sr_pitharan amount > 0 }
		has_resource = { type = sr_engos amount > 0 }
		has_resource = { type = sr_satramene amount > 0 }
		has_resource = { type = sr_terraform_gases amount > 0 }
		has_resource = { type = sr_terraform_liquids amount > 0 }
		has_resource = { type = sr_garanthium amount > 0 }
		has_resource = { type = sr_lythuric amount > 0 }
		has_resource = { type = sr_teldar amount > 0 }
		has_resource = { type = sr_yuranic amount > 0 }
		has_resource = { type = sr_orillium amount > 0 }
		has_resource = { type = sr_neutronium amount > 0 }
		has_resource = { type = sr_living_metal amount > 0 }
		has_resource = { type = sr_zro amount > 0 }
		has_resource = { type = sr_dark_matter amount > 0 }
	}
}		

has_vanilla_orbital_strategic_resource = {
	OR = {
		has_resource = { type = sr_pitharan amount > 0 }
		has_resource = { type = sr_engos amount > 0 }
		has_resource = { type = sr_satramene amount > 0 }
		has_resource = { type = sr_terraform_gases amount > 0 }
		has_resource = { type = sr_terraform_liquids amount > 0 }
		has_resource = { type = sr_garanthium amount > 0 }
		has_resource = { type = sr_lythuric amount > 0 }
		has_resource = { type = sr_teldar amount > 0 }
		has_resource = { type = sr_yuranic amount > 0 }
		has_resource = { type = sr_orillium amount > 0 }
		has_resource = { type = sr_neutronium amount > 0 }
		has_resource = { type = sr_living_metal amount > 0 }
		has_resource = { type = sr_zro amount > 0 }
		has_resource = { type = sr_dark_matter amount > 0 }
	}
}	

is_dry_planet_class = {
	OR = {
		is_planet_class = pc_desert
		is_planet_class = pc_arid
		is_planet_class = pc_savannah
	}
}
	
is_wet_planet_class = {
	OR = {		
		is_planet_class = pc_tropical
		is_planet_class = pc_continental 
		is_planet_class = pc_ocean 
	}
}	

is_cold_planet_class = {
	OR = {
		is_planet_class = pc_tundra
		is_planet_class = pc_arctic
		is_planet_class = pc_alpine
	}
}

is_nuked = { is_planet_class = pc_nuked }
is_gaia = { is_planet_class = pc_gaia }
is_barren = { is_planet_class = pc_barren }
is_barren_cold = { is_planet_class = pc_barren_cold	}
is_gas_giant = { is_planet_class = pc_gas_giant }
is_molten = { is_planet_class = pc_molten }
is_toxic = { is_planet_class = pc_toxic }
is_frozen = { is_planet_class = pc_frozen }

is_collapsed_star = {
	OR = {
		is_planet_class = pc_black_hole
		is_planet_class = pc_neutron_star
		is_planet_class = pc_pulsar
	}
}

is_free_planetary_tile = {
	has_building = no	
	has_deposit = no
	has_blocker = no
}

has_free_planetary_tile = {
	any_tile = {
		has_building = no	
		has_deposit = no
		has_blocker = no
	}	
}

disable_spawn = { #this trigger is used to test engine spawning
	NOT = { has_global_flag = game_started }
}	