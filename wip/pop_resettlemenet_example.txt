	planet_scope = {
		while = { 
			limit = { 
				any_tile = { has_building = yes is_ruined = no has_pop = no }
				any_tile = { has_building = no has_pop = yes pop = { is_colony_pop = yes is_being_purged = no } }
			}	
			every_tile = {
				limit = { has_building = yes has_pop = no }
				prev = { #this being planet again
					random_tile = {
						any_tile = { has_building = no has_pop = yes pop = { is_colony_pop = yes is_being_purged = no } }
						resettle_pop = { pop = pop tile = prevprev }
					}
				}
			}
		}
	}