namespace = custom_difficulty
country_event = {
	id = custom_difficulty.4000
	is_triggered_only = yes
	title = custom_difficulty.player.name
	desc = {
		trigger = {
			text = custom_difficulty.current_bonuses
			text = newline
			fail_text = {
				text = custom_difficulty.player.minerals.desc
				check_variable = { which = custom_difficulty.player.minerals.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.energy.desc
				check_variable = { which = custom_difficulty.player.energy.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.food.desc
				check_variable = { which = custom_difficulty.player.food.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.research.desc
				check_variable = { which = custom_difficulty.player.research.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.unity.desc
				check_variable = { which = custom_difficulty.player.unity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.influence.desc
				check_variable = { which = custom_difficulty.player.influence.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.naval_capacity.desc
				check_variable = { which = custom_difficulty.player.naval_capacity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.weapon_damage.desc
				check_variable = { which = custom_difficulty.player.weapon_damage.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.hull.desc
				check_variable = { which = custom_difficulty.player.hull.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.armor.desc
				check_variable = { which = custom_difficulty.player.armor.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.shield.desc
				check_variable = { which = custom_difficulty.player.shield.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.upkeep.desc
				check_variable = { which = custom_difficulty.player.upkeep.value value = 0 }
			}
		}
	}
	option = {
		name = custom_difficulty.player.change.general_income.name
		hidden_effect = {
			country_event = { id = custom_difficulty.4010 }
		}
	}
	option = {
		name = custom_difficulty.player.change.all_ship_bonuses.name
		hidden_effect = {
			country_event = { id = custom_difficulty.4020 }
		}
	}
	option = {
		name = custom_difficulty.player.change.minerals.name
		hidden_effect = {
			country_event = { id = custom_difficulty.4030 }
		}
	}
	option = {
		name = custom_difficulty.player.change.energy.name
		hidden_effect = {
			country_event = { id = custom_difficulty.4040 }
		}
	}
	option = {
		name = custom_difficulty.player.change.food.name
		hidden_effect = {
			country_event = { id = custom_difficulty.4050 }
		}
	}
	option = {
		name = custom_difficulty.player.change.research.name
		hidden_effect = {
			country_event = { id = custom_difficulty.4060 }
		}
	}
	option = {
		name = custom_difficulty.player.change.unity.name
		hidden_effect = {
			country_event = { id = custom_difficulty.4070 }
		}
	}
	option = {
		name = custom_difficulty.player.change.influence.name
		hidden_effect = {
			country_event = { id = custom_difficulty.4080 }
		}
	}
	option = {
		name = custom_difficulty.player.change.naval_capacity.name
		hidden_effect = {
			country_event = { id = custom_difficulty.4090 }
		}
	}
	option = {
		name = custom_difficulty.player.change.weapon_damage.name
		hidden_effect = {
			country_event = { id = custom_difficulty.4100 }
		}
	}
	option = {
		name = custom_difficulty.player.change.hull.name
		hidden_effect = {
			country_event = { id = custom_difficulty.4110 }
		}
	}
	option = {
		name = custom_difficulty.player.change.armor.name
		hidden_effect = {
			country_event = { id = custom_difficulty.4120 }
		}
	}
	option = {
		name = custom_difficulty.player.change.shield.name
		hidden_effect = {
			country_event = { id = custom_difficulty.4130 }
		}
	}
	option = {
		name = custom_difficulty.player.change.upkeep.name
		hidden_effect = {
			country_event = { id = custom_difficulty.4140 }
		}
	}
	option = {
		name = custom_difficulty.back
		hidden_effect = {
			country_event = { id = custom_difficulty.0 }
		}
	}
	option = {
		name = close_custom_difficulty.name
		hidden_effect = {
			country_event = { id = custom_difficulty.9999 }
		}
	}
}
country_event = {
	id = custom_difficulty.4010
	is_triggered_only = yes
	title = custom_difficulty.player.change.general_income.name
	desc = {
		trigger = {
			text = custom_difficulty.current_bonuses
			text = newline
			fail_text = {
				text = custom_difficulty.player.minerals.desc
				check_variable = { which = custom_difficulty.player.minerals.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.energy.desc
				check_variable = { which = custom_difficulty.player.energy.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.food.desc
				check_variable = { which = custom_difficulty.player.food.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.research.desc
				check_variable = { which = custom_difficulty.player.research.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.unity.desc
				check_variable = { which = custom_difficulty.player.unity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.influence.desc
				check_variable = { which = custom_difficulty.player.influence.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.naval_capacity.desc
				check_variable = { which = custom_difficulty.player.naval_capacity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.weapon_damage.desc
				check_variable = { which = custom_difficulty.player.weapon_damage.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.hull.desc
				check_variable = { which = custom_difficulty.player.hull.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.armor.desc
				check_variable = { which = custom_difficulty.player.armor.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.shield.desc
				check_variable = { which = custom_difficulty.player.shield.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.upkeep.desc
				check_variable = { which = custom_difficulty.player.upkeep.value value = 0 }
			}
		}
	}
	option = {
		name = custom_difficulty.general_income.increase.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.general_income.value value = 50 }
			country_event = { id = custom_difficulty.4010 }
		}
	}
	option = {
		name = custom_difficulty.general_income.increase.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.general_income.value value = 10 }
			country_event = { id = custom_difficulty.4010 }
		}
	}
	option = {
		name = custom_difficulty.general_income.decrease.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.general_income.value value = -10 }
			country_event = { id = custom_difficulty.4010 }
		}
	}
	option = {
		name = custom_difficulty.general_income.decrease.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.general_income.value value = -50 }
			country_event = { id = custom_difficulty.4010 }
		}
	}
	option = {
		name = custom_difficulty.back
		hidden_effect = {
			country_event = { id = custom_difficulty.4000 }
		}
	}
	option = {
		name = close_custom_difficulty.name
		hidden_effect = {
			country_event = { id = custom_difficulty.9999 }
		}
	}
}
country_event = {
	id = custom_difficulty.4020
	is_triggered_only = yes
	title = custom_difficulty.player.change.all_ship_bonuses.name
	desc = {
		trigger = {
			text = custom_difficulty.current_bonuses
			text = newline
			fail_text = {
				text = custom_difficulty.player.minerals.desc
				check_variable = { which = custom_difficulty.player.minerals.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.energy.desc
				check_variable = { which = custom_difficulty.player.energy.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.food.desc
				check_variable = { which = custom_difficulty.player.food.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.research.desc
				check_variable = { which = custom_difficulty.player.research.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.unity.desc
				check_variable = { which = custom_difficulty.player.unity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.influence.desc
				check_variable = { which = custom_difficulty.player.influence.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.naval_capacity.desc
				check_variable = { which = custom_difficulty.player.naval_capacity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.weapon_damage.desc
				check_variable = { which = custom_difficulty.player.weapon_damage.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.hull.desc
				check_variable = { which = custom_difficulty.player.hull.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.armor.desc
				check_variable = { which = custom_difficulty.player.armor.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.shield.desc
				check_variable = { which = custom_difficulty.player.shield.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.upkeep.desc
				check_variable = { which = custom_difficulty.player.upkeep.value value = 0 }
			}
		}
	}
	option = {
		name = custom_difficulty.all_ship_bonuses.increase.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.all_ship_bonuses.value value = 50 }
			country_event = { id = custom_difficulty.4020 }
		}
	}
	option = {
		name = custom_difficulty.all_ship_bonuses.increase.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.all_ship_bonuses.value value = 10 }
			country_event = { id = custom_difficulty.4020 }
		}
	}
	option = {
		name = custom_difficulty.all_ship_bonuses.decrease.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.all_ship_bonuses.value value = -10 }
			country_event = { id = custom_difficulty.4020 }
		}
	}
	option = {
		name = custom_difficulty.all_ship_bonuses.decrease.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.all_ship_bonuses.value value = -50 }
			country_event = { id = custom_difficulty.4020 }
		}
	}
	option = {
		name = custom_difficulty.back
		hidden_effect = {
			country_event = { id = custom_difficulty.4000 }
		}
	}
	option = {
		name = close_custom_difficulty.name
		hidden_effect = {
			country_event = { id = custom_difficulty.9999 }
		}
	}
}
country_event = {
	id = custom_difficulty.4030
	is_triggered_only = yes
	title = custom_difficulty.player.change.minerals.name
	desc = {
		trigger = {
			text = custom_difficulty.current_bonuses
			text = newline
			fail_text = {
				text = custom_difficulty.player.minerals.desc
				check_variable = { which = custom_difficulty.player.minerals.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.energy.desc
				check_variable = { which = custom_difficulty.player.energy.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.food.desc
				check_variable = { which = custom_difficulty.player.food.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.research.desc
				check_variable = { which = custom_difficulty.player.research.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.unity.desc
				check_variable = { which = custom_difficulty.player.unity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.influence.desc
				check_variable = { which = custom_difficulty.player.influence.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.naval_capacity.desc
				check_variable = { which = custom_difficulty.player.naval_capacity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.weapon_damage.desc
				check_variable = { which = custom_difficulty.player.weapon_damage.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.hull.desc
				check_variable = { which = custom_difficulty.player.hull.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.armor.desc
				check_variable = { which = custom_difficulty.player.armor.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.shield.desc
				check_variable = { which = custom_difficulty.player.shield.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.upkeep.desc
				check_variable = { which = custom_difficulty.player.upkeep.value value = 0 }
			}
		}
	}
	option = {
		name = custom_difficulty.minerals.increase.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.minerals.value value = 50 }
			country_event = { id = custom_difficulty.4030 }
		}
	}
	option = {
		name = custom_difficulty.minerals.increase.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.minerals.value value = 10 }
			country_event = { id = custom_difficulty.4030 }
		}
	}
	option = {
		name = custom_difficulty.minerals.decrease.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.minerals.value value = -10 }
			country_event = { id = custom_difficulty.4030 }
		}
	}
	option = {
		name = custom_difficulty.minerals.decrease.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.minerals.value value = -50 }
			country_event = { id = custom_difficulty.4030 }
		}
	}
	option = {
		name = custom_difficulty.back
		hidden_effect = {
			country_event = { id = custom_difficulty.4000 }
		}
	}
	option = {
		name = close_custom_difficulty.name
		hidden_effect = {
			country_event = { id = custom_difficulty.9999 }
		}
	}
}
country_event = {
	id = custom_difficulty.4040
	is_triggered_only = yes
	title = custom_difficulty.player.change.energy.name
	desc = {
		trigger = {
			text = custom_difficulty.current_bonuses
			text = newline
			fail_text = {
				text = custom_difficulty.player.minerals.desc
				check_variable = { which = custom_difficulty.player.minerals.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.energy.desc
				check_variable = { which = custom_difficulty.player.energy.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.food.desc
				check_variable = { which = custom_difficulty.player.food.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.research.desc
				check_variable = { which = custom_difficulty.player.research.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.unity.desc
				check_variable = { which = custom_difficulty.player.unity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.influence.desc
				check_variable = { which = custom_difficulty.player.influence.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.naval_capacity.desc
				check_variable = { which = custom_difficulty.player.naval_capacity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.weapon_damage.desc
				check_variable = { which = custom_difficulty.player.weapon_damage.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.hull.desc
				check_variable = { which = custom_difficulty.player.hull.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.armor.desc
				check_variable = { which = custom_difficulty.player.armor.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.shield.desc
				check_variable = { which = custom_difficulty.player.shield.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.upkeep.desc
				check_variable = { which = custom_difficulty.player.upkeep.value value = 0 }
			}
		}
	}
	option = {
		name = custom_difficulty.energy.increase.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.energy.value value = 50 }
			country_event = { id = custom_difficulty.4040 }
		}
	}
	option = {
		name = custom_difficulty.energy.increase.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.energy.value value = 10 }
			country_event = { id = custom_difficulty.4040 }
		}
	}
	option = {
		name = custom_difficulty.energy.decrease.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.energy.value value = -10 }
			country_event = { id = custom_difficulty.4040 }
		}
	}
	option = {
		name = custom_difficulty.energy.decrease.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.energy.value value = -50 }
			country_event = { id = custom_difficulty.4040 }
		}
	}
	option = {
		name = custom_difficulty.back
		hidden_effect = {
			country_event = { id = custom_difficulty.4000 }
		}
	}
	option = {
		name = close_custom_difficulty.name
		hidden_effect = {
			country_event = { id = custom_difficulty.9999 }
		}
	}
}
country_event = {
	id = custom_difficulty.4050
	is_triggered_only = yes
	title = custom_difficulty.player.change.food.name
	desc = {
		trigger = {
			text = custom_difficulty.current_bonuses
			text = newline
			fail_text = {
				text = custom_difficulty.player.minerals.desc
				check_variable = { which = custom_difficulty.player.minerals.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.energy.desc
				check_variable = { which = custom_difficulty.player.energy.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.food.desc
				check_variable = { which = custom_difficulty.player.food.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.research.desc
				check_variable = { which = custom_difficulty.player.research.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.unity.desc
				check_variable = { which = custom_difficulty.player.unity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.influence.desc
				check_variable = { which = custom_difficulty.player.influence.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.naval_capacity.desc
				check_variable = { which = custom_difficulty.player.naval_capacity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.weapon_damage.desc
				check_variable = { which = custom_difficulty.player.weapon_damage.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.hull.desc
				check_variable = { which = custom_difficulty.player.hull.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.armor.desc
				check_variable = { which = custom_difficulty.player.armor.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.shield.desc
				check_variable = { which = custom_difficulty.player.shield.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.upkeep.desc
				check_variable = { which = custom_difficulty.player.upkeep.value value = 0 }
			}
		}
	}
	option = {
		name = custom_difficulty.food.increase.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.food.value value = 50 }
			country_event = { id = custom_difficulty.4050 }
		}
	}
	option = {
		name = custom_difficulty.food.increase.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.food.value value = 10 }
			country_event = { id = custom_difficulty.4050 }
		}
	}
	option = {
		name = custom_difficulty.food.decrease.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.food.value value = -10 }
			country_event = { id = custom_difficulty.4050 }
		}
	}
	option = {
		name = custom_difficulty.food.decrease.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.food.value value = -50 }
			country_event = { id = custom_difficulty.4050 }
		}
	}
	option = {
		name = custom_difficulty.back
		hidden_effect = {
			country_event = { id = custom_difficulty.4000 }
		}
	}
	option = {
		name = close_custom_difficulty.name
		hidden_effect = {
			country_event = { id = custom_difficulty.9999 }
		}
	}
}
country_event = {
	id = custom_difficulty.4060
	is_triggered_only = yes
	title = custom_difficulty.player.change.research.name
	desc = {
		trigger = {
			text = custom_difficulty.current_bonuses
			text = newline
			fail_text = {
				text = custom_difficulty.player.minerals.desc
				check_variable = { which = custom_difficulty.player.minerals.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.energy.desc
				check_variable = { which = custom_difficulty.player.energy.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.food.desc
				check_variable = { which = custom_difficulty.player.food.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.research.desc
				check_variable = { which = custom_difficulty.player.research.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.unity.desc
				check_variable = { which = custom_difficulty.player.unity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.influence.desc
				check_variable = { which = custom_difficulty.player.influence.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.naval_capacity.desc
				check_variable = { which = custom_difficulty.player.naval_capacity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.weapon_damage.desc
				check_variable = { which = custom_difficulty.player.weapon_damage.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.hull.desc
				check_variable = { which = custom_difficulty.player.hull.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.armor.desc
				check_variable = { which = custom_difficulty.player.armor.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.shield.desc
				check_variable = { which = custom_difficulty.player.shield.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.upkeep.desc
				check_variable = { which = custom_difficulty.player.upkeep.value value = 0 }
			}
		}
	}
	option = {
		name = custom_difficulty.research.increase.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.research.value value = 50 }
			country_event = { id = custom_difficulty.4060 }
		}
	}
	option = {
		name = custom_difficulty.research.increase.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.research.value value = 10 }
			country_event = { id = custom_difficulty.4060 }
		}
	}
	option = {
		name = custom_difficulty.research.decrease.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.research.value value = -10 }
			country_event = { id = custom_difficulty.4060 }
		}
	}
	option = {
		name = custom_difficulty.research.decrease.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.research.value value = -50 }
			country_event = { id = custom_difficulty.4060 }
		}
	}
	option = {
		name = custom_difficulty.back
		hidden_effect = {
			country_event = { id = custom_difficulty.4000 }
		}
	}
	option = {
		name = close_custom_difficulty.name
		hidden_effect = {
			country_event = { id = custom_difficulty.9999 }
		}
	}
}
country_event = {
	id = custom_difficulty.4070
	is_triggered_only = yes
	title = custom_difficulty.player.change.unity.name
	desc = {
		trigger = {
			text = custom_difficulty.current_bonuses
			text = newline
			fail_text = {
				text = custom_difficulty.player.minerals.desc
				check_variable = { which = custom_difficulty.player.minerals.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.energy.desc
				check_variable = { which = custom_difficulty.player.energy.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.food.desc
				check_variable = { which = custom_difficulty.player.food.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.research.desc
				check_variable = { which = custom_difficulty.player.research.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.unity.desc
				check_variable = { which = custom_difficulty.player.unity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.influence.desc
				check_variable = { which = custom_difficulty.player.influence.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.naval_capacity.desc
				check_variable = { which = custom_difficulty.player.naval_capacity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.weapon_damage.desc
				check_variable = { which = custom_difficulty.player.weapon_damage.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.hull.desc
				check_variable = { which = custom_difficulty.player.hull.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.armor.desc
				check_variable = { which = custom_difficulty.player.armor.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.shield.desc
				check_variable = { which = custom_difficulty.player.shield.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.upkeep.desc
				check_variable = { which = custom_difficulty.player.upkeep.value value = 0 }
			}
		}
	}
	option = {
		name = custom_difficulty.unity.increase.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.unity.value value = 50 }
			country_event = { id = custom_difficulty.4070 }
		}
	}
	option = {
		name = custom_difficulty.unity.increase.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.unity.value value = 10 }
			country_event = { id = custom_difficulty.4070 }
		}
	}
	option = {
		name = custom_difficulty.unity.decrease.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.unity.value value = -10 }
			country_event = { id = custom_difficulty.4070 }
		}
	}
	option = {
		name = custom_difficulty.unity.decrease.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.unity.value value = -50 }
			country_event = { id = custom_difficulty.4070 }
		}
	}
	option = {
		name = custom_difficulty.back
		hidden_effect = {
			country_event = { id = custom_difficulty.4000 }
		}
	}
	option = {
		name = close_custom_difficulty.name
		hidden_effect = {
			country_event = { id = custom_difficulty.9999 }
		}
	}
}
country_event = {
	id = custom_difficulty.4080
	is_triggered_only = yes
	title = custom_difficulty.player.change.influence.name
	desc = {
		trigger = {
			text = custom_difficulty.current_bonuses
			text = newline
			fail_text = {
				text = custom_difficulty.player.minerals.desc
				check_variable = { which = custom_difficulty.player.minerals.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.energy.desc
				check_variable = { which = custom_difficulty.player.energy.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.food.desc
				check_variable = { which = custom_difficulty.player.food.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.research.desc
				check_variable = { which = custom_difficulty.player.research.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.unity.desc
				check_variable = { which = custom_difficulty.player.unity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.influence.desc
				check_variable = { which = custom_difficulty.player.influence.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.naval_capacity.desc
				check_variable = { which = custom_difficulty.player.naval_capacity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.weapon_damage.desc
				check_variable = { which = custom_difficulty.player.weapon_damage.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.hull.desc
				check_variable = { which = custom_difficulty.player.hull.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.armor.desc
				check_variable = { which = custom_difficulty.player.armor.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.shield.desc
				check_variable = { which = custom_difficulty.player.shield.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.upkeep.desc
				check_variable = { which = custom_difficulty.player.upkeep.value value = 0 }
			}
		}
	}
	option = {
		name = custom_difficulty.influence.increase.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.influence.value value = 50 }
			country_event = { id = custom_difficulty.4080 }
		}
	}
	option = {
		name = custom_difficulty.influence.increase.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.influence.value value = 10 }
			country_event = { id = custom_difficulty.4080 }
		}
	}
	option = {
		name = custom_difficulty.influence.decrease.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.influence.value value = -10 }
			country_event = { id = custom_difficulty.4080 }
		}
	}
	option = {
		name = custom_difficulty.influence.decrease.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.influence.value value = -50 }
			country_event = { id = custom_difficulty.4080 }
		}
	}
	option = {
		name = custom_difficulty.back
		hidden_effect = {
			country_event = { id = custom_difficulty.4000 }
		}
	}
	option = {
		name = close_custom_difficulty.name
		hidden_effect = {
			country_event = { id = custom_difficulty.9999 }
		}
	}
}
country_event = {
	id = custom_difficulty.4090
	is_triggered_only = yes
	title = custom_difficulty.player.change.naval_capacity.name
	desc = {
		trigger = {
			text = custom_difficulty.current_bonuses
			text = newline
			fail_text = {
				text = custom_difficulty.player.minerals.desc
				check_variable = { which = custom_difficulty.player.minerals.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.energy.desc
				check_variable = { which = custom_difficulty.player.energy.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.food.desc
				check_variable = { which = custom_difficulty.player.food.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.research.desc
				check_variable = { which = custom_difficulty.player.research.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.unity.desc
				check_variable = { which = custom_difficulty.player.unity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.influence.desc
				check_variable = { which = custom_difficulty.player.influence.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.naval_capacity.desc
				check_variable = { which = custom_difficulty.player.naval_capacity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.weapon_damage.desc
				check_variable = { which = custom_difficulty.player.weapon_damage.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.hull.desc
				check_variable = { which = custom_difficulty.player.hull.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.armor.desc
				check_variable = { which = custom_difficulty.player.armor.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.shield.desc
				check_variable = { which = custom_difficulty.player.shield.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.upkeep.desc
				check_variable = { which = custom_difficulty.player.upkeep.value value = 0 }
			}
		}
	}
	option = {
		name = custom_difficulty.naval_capacity.increase.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.naval_capacity.value value = 50 }
			country_event = { id = custom_difficulty.4090 }
		}
	}
	option = {
		name = custom_difficulty.naval_capacity.increase.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.naval_capacity.value value = 10 }
			country_event = { id = custom_difficulty.4090 }
		}
	}
	option = {
		name = custom_difficulty.naval_capacity.decrease.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.naval_capacity.value value = -10 }
			country_event = { id = custom_difficulty.4090 }
		}
	}
	option = {
		name = custom_difficulty.naval_capacity.decrease.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.naval_capacity.value value = -50 }
			country_event = { id = custom_difficulty.4090 }
		}
	}
	option = {
		name = custom_difficulty.back
		hidden_effect = {
			country_event = { id = custom_difficulty.4000 }
		}
	}
	option = {
		name = close_custom_difficulty.name
		hidden_effect = {
			country_event = { id = custom_difficulty.9999 }
		}
	}
}
country_event = {
	id = custom_difficulty.4100
	is_triggered_only = yes
	title = custom_difficulty.player.change.weapon_damage.name
	desc = {
		trigger = {
			text = custom_difficulty.current_bonuses
			text = newline
			fail_text = {
				text = custom_difficulty.player.minerals.desc
				check_variable = { which = custom_difficulty.player.minerals.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.energy.desc
				check_variable = { which = custom_difficulty.player.energy.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.food.desc
				check_variable = { which = custom_difficulty.player.food.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.research.desc
				check_variable = { which = custom_difficulty.player.research.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.unity.desc
				check_variable = { which = custom_difficulty.player.unity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.influence.desc
				check_variable = { which = custom_difficulty.player.influence.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.naval_capacity.desc
				check_variable = { which = custom_difficulty.player.naval_capacity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.weapon_damage.desc
				check_variable = { which = custom_difficulty.player.weapon_damage.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.hull.desc
				check_variable = { which = custom_difficulty.player.hull.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.armor.desc
				check_variable = { which = custom_difficulty.player.armor.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.shield.desc
				check_variable = { which = custom_difficulty.player.shield.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.upkeep.desc
				check_variable = { which = custom_difficulty.player.upkeep.value value = 0 }
			}
		}
	}
	option = {
		name = custom_difficulty.weapon_damage.increase.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.weapon_damage.value value = 50 }
			country_event = { id = custom_difficulty.4100 }
		}
	}
	option = {
		name = custom_difficulty.weapon_damage.increase.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.weapon_damage.value value = 10 }
			country_event = { id = custom_difficulty.4100 }
		}
	}
	option = {
		name = custom_difficulty.weapon_damage.decrease.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.weapon_damage.value value = -10 }
			country_event = { id = custom_difficulty.4100 }
		}
	}
	option = {
		name = custom_difficulty.weapon_damage.decrease.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.weapon_damage.value value = -50 }
			country_event = { id = custom_difficulty.4100 }
		}
	}
	option = {
		name = custom_difficulty.back
		hidden_effect = {
			country_event = { id = custom_difficulty.4000 }
		}
	}
	option = {
		name = close_custom_difficulty.name
		hidden_effect = {
			country_event = { id = custom_difficulty.9999 }
		}
	}
}
country_event = {
	id = custom_difficulty.4110
	is_triggered_only = yes
	title = custom_difficulty.player.change.hull.name
	desc = {
		trigger = {
			text = custom_difficulty.current_bonuses
			text = newline
			fail_text = {
				text = custom_difficulty.player.minerals.desc
				check_variable = { which = custom_difficulty.player.minerals.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.energy.desc
				check_variable = { which = custom_difficulty.player.energy.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.food.desc
				check_variable = { which = custom_difficulty.player.food.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.research.desc
				check_variable = { which = custom_difficulty.player.research.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.unity.desc
				check_variable = { which = custom_difficulty.player.unity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.influence.desc
				check_variable = { which = custom_difficulty.player.influence.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.naval_capacity.desc
				check_variable = { which = custom_difficulty.player.naval_capacity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.weapon_damage.desc
				check_variable = { which = custom_difficulty.player.weapon_damage.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.hull.desc
				check_variable = { which = custom_difficulty.player.hull.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.armor.desc
				check_variable = { which = custom_difficulty.player.armor.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.shield.desc
				check_variable = { which = custom_difficulty.player.shield.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.upkeep.desc
				check_variable = { which = custom_difficulty.player.upkeep.value value = 0 }
			}
		}
	}
	option = {
		name = custom_difficulty.hull.increase.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.hull.value value = 50 }
			country_event = { id = custom_difficulty.4110 }
		}
	}
	option = {
		name = custom_difficulty.hull.increase.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.hull.value value = 10 }
			country_event = { id = custom_difficulty.4110 }
		}
	}
	option = {
		name = custom_difficulty.hull.decrease.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.hull.value value = -10 }
			country_event = { id = custom_difficulty.4110 }
		}
	}
	option = {
		name = custom_difficulty.hull.decrease.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.hull.value value = -50 }
			country_event = { id = custom_difficulty.4110 }
		}
	}
	option = {
		name = custom_difficulty.back
		hidden_effect = {
			country_event = { id = custom_difficulty.4000 }
		}
	}
	option = {
		name = close_custom_difficulty.name
		hidden_effect = {
			country_event = { id = custom_difficulty.9999 }
		}
	}
}
country_event = {
	id = custom_difficulty.4120
	is_triggered_only = yes
	title = custom_difficulty.player.change.armor.name
	desc = {
		trigger = {
			text = custom_difficulty.current_bonuses
			text = newline
			fail_text = {
				text = custom_difficulty.player.minerals.desc
				check_variable = { which = custom_difficulty.player.minerals.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.energy.desc
				check_variable = { which = custom_difficulty.player.energy.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.food.desc
				check_variable = { which = custom_difficulty.player.food.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.research.desc
				check_variable = { which = custom_difficulty.player.research.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.unity.desc
				check_variable = { which = custom_difficulty.player.unity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.influence.desc
				check_variable = { which = custom_difficulty.player.influence.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.naval_capacity.desc
				check_variable = { which = custom_difficulty.player.naval_capacity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.weapon_damage.desc
				check_variable = { which = custom_difficulty.player.weapon_damage.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.hull.desc
				check_variable = { which = custom_difficulty.player.hull.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.armor.desc
				check_variable = { which = custom_difficulty.player.armor.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.shield.desc
				check_variable = { which = custom_difficulty.player.shield.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.upkeep.desc
				check_variable = { which = custom_difficulty.player.upkeep.value value = 0 }
			}
		}
	}
	option = {
		name = custom_difficulty.armor.increase.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.armor.value value = 50 }
			country_event = { id = custom_difficulty.4120 }
		}
	}
	option = {
		name = custom_difficulty.armor.increase.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.armor.value value = 10 }
			country_event = { id = custom_difficulty.4120 }
		}
	}
	option = {
		name = custom_difficulty.armor.decrease.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.armor.value value = -10 }
			country_event = { id = custom_difficulty.4120 }
		}
	}
	option = {
		name = custom_difficulty.armor.decrease.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.armor.value value = -50 }
			country_event = { id = custom_difficulty.4120 }
		}
	}
	option = {
		name = custom_difficulty.back
		hidden_effect = {
			country_event = { id = custom_difficulty.4000 }
		}
	}
	option = {
		name = close_custom_difficulty.name
		hidden_effect = {
			country_event = { id = custom_difficulty.9999 }
		}
	}
}
country_event = {
	id = custom_difficulty.4130
	is_triggered_only = yes
	title = custom_difficulty.player.change.shield.name
	desc = {
		trigger = {
			text = custom_difficulty.current_bonuses
			text = newline
			fail_text = {
				text = custom_difficulty.player.minerals.desc
				check_variable = { which = custom_difficulty.player.minerals.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.energy.desc
				check_variable = { which = custom_difficulty.player.energy.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.food.desc
				check_variable = { which = custom_difficulty.player.food.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.research.desc
				check_variable = { which = custom_difficulty.player.research.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.unity.desc
				check_variable = { which = custom_difficulty.player.unity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.influence.desc
				check_variable = { which = custom_difficulty.player.influence.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.naval_capacity.desc
				check_variable = { which = custom_difficulty.player.naval_capacity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.weapon_damage.desc
				check_variable = { which = custom_difficulty.player.weapon_damage.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.hull.desc
				check_variable = { which = custom_difficulty.player.hull.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.armor.desc
				check_variable = { which = custom_difficulty.player.armor.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.shield.desc
				check_variable = { which = custom_difficulty.player.shield.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.upkeep.desc
				check_variable = { which = custom_difficulty.player.upkeep.value value = 0 }
			}
		}
	}
	option = {
		name = custom_difficulty.shield.increase.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.shield.value value = 50 }
			country_event = { id = custom_difficulty.4130 }
		}
	}
	option = {
		name = custom_difficulty.shield.increase.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.shield.value value = 10 }
			country_event = { id = custom_difficulty.4130 }
		}
	}
	option = {
		name = custom_difficulty.shield.decrease.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.shield.value value = -10 }
			country_event = { id = custom_difficulty.4130 }
		}
	}
	option = {
		name = custom_difficulty.shield.decrease.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.shield.value value = -50 }
			country_event = { id = custom_difficulty.4130 }
		}
	}
	option = {
		name = custom_difficulty.back
		hidden_effect = {
			country_event = { id = custom_difficulty.4000 }
		}
	}
	option = {
		name = close_custom_difficulty.name
		hidden_effect = {
			country_event = { id = custom_difficulty.9999 }
		}
	}
}
country_event = {
	id = custom_difficulty.4140
	is_triggered_only = yes
	title = custom_difficulty.player.change.upkeep.name
	desc = {
		trigger = {
			text = custom_difficulty.current_bonuses
			text = newline
			fail_text = {
				text = custom_difficulty.player.minerals.desc
				check_variable = { which = custom_difficulty.player.minerals.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.energy.desc
				check_variable = { which = custom_difficulty.player.energy.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.food.desc
				check_variable = { which = custom_difficulty.player.food.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.research.desc
				check_variable = { which = custom_difficulty.player.research.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.unity.desc
				check_variable = { which = custom_difficulty.player.unity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.influence.desc
				check_variable = { which = custom_difficulty.player.influence.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.naval_capacity.desc
				check_variable = { which = custom_difficulty.player.naval_capacity.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.weapon_damage.desc
				check_variable = { which = custom_difficulty.player.weapon_damage.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.hull.desc
				check_variable = { which = custom_difficulty.player.hull.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.armor.desc
				check_variable = { which = custom_difficulty.player.armor.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.shield.desc
				check_variable = { which = custom_difficulty.player.shield.value value = 0 }
			}
			fail_text = {
				text = custom_difficulty.player.upkeep.desc
				check_variable = { which = custom_difficulty.player.upkeep.value value = 0 }
			}
		}
	}
	option = {
		name = custom_difficulty.upkeep.increase.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.upkeep.value value = 50 }
			country_event = { id = custom_difficulty.4140 }
		}
	}
	option = {
		name = custom_difficulty.upkeep.increase.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.upkeep.value value = 10 }
			country_event = { id = custom_difficulty.4140 }
		}
	}
	option = {
		name = custom_difficulty.upkeep.decrease.10
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.upkeep.value value = -10 }
			country_event = { id = custom_difficulty.4140 }
		}
	}
	option = {
		name = custom_difficulty.upkeep.decrease.50
		hidden_effect = {
			change_variable = { which = custom_difficulty.player.upkeep.value value = -50 }
			country_event = { id = custom_difficulty.4140 }
		}
	}
	option = {
		name = custom_difficulty.back
		hidden_effect = {
			country_event = { id = custom_difficulty.4000 }
		}
	}
	option = {
		name = close_custom_difficulty.name
		hidden_effect = {
			country_event = { id = custom_difficulty.9999 }
		}
	}
}
