namespace = test_events

event = { #tier 1 tech unlock event
	id = test_events.1
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		every_country = {
			limit = { is_country_type = default	}				
			#### place the give_technology list here - same tab as the limit field above like this
			# give_technology = { message = no tech_xyz1 }
			# give_technology = { message = no tech_xyz2 }
			# give_technology = { message = no tech_xyz3 }
		}
	}
}

event = { #tier 2 tech unlock event
	id = test_events.2
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		every_country = {
			limit = { is_country_type = default	}				
			#### place the give_technology list here - same tab as the limit field above like this
			# give_technology = { message = no tech_xyz1 }
			# give_technology = { message = no tech_xyz2 }
			# give_technology = { message = no tech_xyz3 }
		}
	}
}

event = { #tier 3 tech unlock event
	id = test_events.3
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		every_country = {
			limit = { is_country_type = default	}				
			#### place the give_technology list here - same tab as the limit field above like this
			# give_technology = { message = no tech_xyz1 }
			# give_technology = { message = no tech_xyz2 }
			# give_technology = { message = no tech_xyz3 }
		}
	}
}

event = { #tier 4 tech unlock event
	id = test_events.4
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		every_country = {
			limit = { is_country_type = default	}				
			#### place the give_technology list here - same tab as the limit field above like this
			# give_technology = { message = no tech_xyz1 }
			# give_technology = { message = no tech_xyz2 }
			# give_technology = { message = no tech_xyz3 }
		}
	}
}