namespace = planet_spawn_engine_beta

event = { 
	id = planet_spawn_engine_beta.1
	hide_window = yes
	is_triggered_only = yes
	fire_only_once = yes
	
	#This event fires only once.
		
	immediate = {		
		every_planet = { 
			if = {
				limit = {
					is_colony = no
					is_planet_class = pc_continental
				}	
				random_list = {
					25 = { set_planet_entity = { entity = "hajungle_planet_01_entity" } }
					25 = { set_planet_entity = { entity = "mangrove_planet_01_entity" } }
					50 = { }					
				}					
			}							
		}
	}
}