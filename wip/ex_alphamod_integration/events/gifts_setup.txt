namespace = gifts_setup

event = {
	id = gifts_setup.1

	is_triggered_only = yes
	hide_window = yes
	
	immediate = {
		every_country = {
			limit = {
				OR = {
					has_trait = "trait_agrarian"
					has_trait = "trait_conservational"
					has_civic = civic_environmentalist
					has_civic = civic_agrarian_idyll
				}
			}
			give_technology = { message = no tech = tech_alpha_traditional_farming }
		}
		every_country = {
			limit = {
				has_civic = civic_environmentalist
			}
			give_technology = { message = no tech = tech_alpha_preservations }
		}
		every_country = {
			limit = {
				OR = {
					has_trait = "trait_industrious"
					has_civic = civic_mining_guilds
				}
			}
			give_technology = { message = no tech = tech_alpha_natfuel_sourcing_2 }
		}			
	}
}