namespace = ex_rw_events

planet_event = {
	id = ex_rw_events.1
	hide_window = yes
	
	trigger = {
		root = {
			is_planet_class = pc_ringworld_habitable
			NOT = { has_modifier = ex_ring_world }	
		}	
	}	
	
	immediate = {
		root = { add_modifier = { modifier = "ex_ring_world" days = -1 } }												
	}
}