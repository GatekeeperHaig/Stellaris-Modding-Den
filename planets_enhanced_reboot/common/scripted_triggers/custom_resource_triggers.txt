### you can insert any of the triggers below (and any other of course) into any of the conditions below changing no/yes - as you see fit.
### to check country scope to owner scope 
### to check mining/research station use orbit scope
#######
	#has_planet_flag = flag_name
	#has_global_flag = flag_name
	#is_modifier_enabled = no
	#is_unique_sr_enabled = no
	#is_orbital_sr_enabled = no
	#is_planetary_sr_enabled = no	
	#is_ringworld = no
	#owner = { is_primitive = no }
	#owner = { is_country_type = default }
	#owner = { OR = { is_country_type = fallen_empire is_country_type = awakened_fallen_empire } }	
	#has_owner = no
	#is_colony = no
	#is_colonizable = no
	#is_homeworld = no
	#is_capital = no	
	#has_pop = no
	#has_building = no
	#is_terraformed = no
	#starting_system = no
	#use this check to find/restrict unique systems, e.g. leviathan systems, custom homeworlds etc. 
	#you can use #OR/AND/NOT/NOR/NAND, for example: ==> 
		#solar_system = { 	
			#OR = {
				#has_star_flag = xyz_flag_name_1
				#has_star_flag = xyz_flag_name_2
				#has_star_flag = xyz_flag_name_3
				#etc.
			#}
		#}	
	#EXAMPLE:
		#solar_system = { 		
			#NOR = {
				#has_star_flag = empire_home_system
				#has_star_flag = sol_system
				#has_star_flag = deneb_system		
			#}	
		#}	
	
#### To restrict to particular planet types use "=". each check is for a single planet class using the OR field below!	
#OR = { 
		#check_variable = { which = "planet_group_id" value = 0.1 } - if you use "=" you are restricting this to a particular planet type
		#check_variable = { which = "planet_group_id" value = 1.1 } - if you use "=" you are restricting this to a particular planet type
		#check_variable = { which = "planet_group_id" value = 1.2 } - if you use "=" you are restricting this to a particular planet type
		#check_variable = { which = "planet_group_id" value = 1.3 } - if you use "=" you are restricting this to a particular planet type		
	#}
#}	

## Note: #you can use #OR/AND/NOT/NOR/NAND - NOT/NOR/NAND to restrict from particular planet types, AND if you want to add an extra condition for that particular planet class
# AND = { 
 	#check_variable = { which = "planet_group_id" value = 0.1 } 
	#has_building = your_special_building
#}	

#### To restrict to a particular planet group you need to check a number range. You require therefore
#### two checks(!). one check checking value > x and the next check value < y
#AND = {
	#check_variable = { which = "planet_group_id" value > 1 } #if you use "><" you need two checks, and you are limiting to a planet range
	#check_variable = { which = "planet_group_id" value < 2 } #if you use "><" you need two checks, as here, and you are limiting to a planet range
#}

#### To restrict to both particular planets and a specific group, use a combined OR+AND like this:
#OR = { 
		#check_variable = { which = "planet_group_id" value = 0.1 } -
		#check_variable = { which = "planet_group_id" value = 1.1 } - if you use "=" you are restricting this to a particular planet type
		#check_variable = { which = "planet_group_id" value = 1.2 } - if you use "=" you are restricting this to a particular planet type
		#check_variable = { which = "planet_group_id" value = 1.3 } - if you use "=" you are restricting this to a particular planet type		
		#AND = {
			#check_variable = { which = "planet_group_id" value > 1 } #if you use "><" you need two checks, and you are limiting to a planet range
			#check_variable = { which = "planet_group_id" value < 2 } #if you use "><" you need two checks, as here, and you are limiting to a planet range
		#}	
	}
}	

####	
####### replicate the fields below as necessary and fill them with your conditions
## unique resource conditions - please make sure to change the <custom_unique_resource_1/2/3...> to your unique resource name, e.g. sr_xyz
#conditions_custom_unique_resource_1 = {
	#insert any triggers here
#}	

#conditions_custom_unique_resource_2 = {
	#insert any triggers here
#}	

#conditions_custom_unique_resource_3 = {
	#insert any triggers here
#}	

## planetary resource conditions - please make sure to change the <custom_planetary_resource_1/2/3...> to your unique resource name, e.g. sr_xyz
#conditions_custom_planetary_resource_1 = {
	#insert any triggers here
#}	

#conditions_custom_planetary_resource_2 = {
	#insert any triggers here
#}	

#conditions_custom_planetary_resource_3 = {
	#insert any triggers here
#}	

## orbital resource conditions - please make sure to change the <custom_orbital_resource_1/2/3...> to your unique resource name, e.g. sr_xyz
#conditions_custom_orbital_resource_1 = {
	#insert any triggers here
#}	

#conditions_custom_orbital_resource_2 = {
	#insert any triggers here
#}	

#conditions_custom_orbital_resource_3 = {
	#insert any triggers here
#}		