 # This file was created by script!
 # Instead of editing it, you should change the python script.
 # Changes to the file will be overwritten the next time the script is run.
 #game start init
country_event = {
	id = custom_difficultyMM.1
	title = custom_difficulty_init
	trigger = {
		is_ai = no
		not = { has_global_flag = custom_difficultyMM_active }
	}
	immediate = {
		country_event = { id = custom_difficulty.11 }  #randomDiffFireOnlyOnce
		set_country_flag = custom_difficulty_game_host
		random_planet = { save_global_event_target_as = custom_difficultyMM_var_storage }
	}
	after = { hidden_effect = { set_global_flag = custom_difficulty_active } }
}
