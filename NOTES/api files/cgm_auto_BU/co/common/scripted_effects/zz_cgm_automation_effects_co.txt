 # This file was created by script!
 # Instead of editing it, you should change the python script.
 # Changes to the file will be overwritten the next time the script is run.
cgm_add_special_building_co = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
		else = {
			if = {
				limit = {
					owner = {
						check_variable = { which = cgm_special_bestBuilding value = -2113928.733 }
					}
				}
				add_building_construction = building_royal_library
			}
			if = {
				limit = {
					OR = { has_building_construction = yes has_building = yes }
				}
				owner = { set_country_flag = cgm_auto_built }
				else = {
					if = {
						limit = {
							owner = {
								check_variable = { which = cgm_special_bestBuilding value = -2080374.301 }
							}
						}
						add_building_construction = building_symbol_purity
					}
					if = {
						limit = {
							OR = { has_building_construction = yes has_building = yes }
						}
						owner = { set_country_flag = cgm_auto_built }
						else = {
							if = {
								limit = {
									owner = {
										check_variable = { which = cgm_special_bestBuilding value = -2046819.869 }
									}
								}
								add_building_construction = building_visitor_center
							}
							if = {
								limit = {
									OR = { has_building_construction = yes has_building = yes }
								}
								owner = { set_country_flag = cgm_auto_built }
								else = {
									if = {
										limit = {
											owner = {
												check_variable = { which = cgm_special_bestBuilding value = -2013265.437 }
											}
										}
										add_building_construction = building_temple
									}
								}
							}
						}
					}
				}
			}
		}
	}
}
cgm_search_for_special_building_co = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
		else = {
			if = {
				limit = {
					exists = owner
					owner = { has_valid_civic = civic_philosopher_king }
					exists = ruler
					ruler = { NOT = { has_leader_flag = built_monument } }
					NOR = { has_building = building_royal_library }
					owner = {
						check_variable = { which = cgm_special_bestWeight value < 10 }
					}
				}
				prev = {
					set_variable = { which = cgm_special_bestWeight value = 10 }
					set_variable = { which = cgm_special_bestBuilding value = -2113928.733 }
				}
				save_event_target_as = cgm_best_planet_for_special
			}
			if = {
				limit = {
					OR = { has_building_construction = yes has_building = yes }
				}
				owner = { set_country_flag = cgm_auto_built }
				else = {
					if = {
						limit = {
							owner = { has_swapped_tradition = tr_purity_keepers_tradition }
							NOR = { has_building = building_symbol_purity }
							owner = {
								check_variable = { which = cgm_special_bestWeight value < 10 }
							}
						}
						prev = {
							set_variable = { which = cgm_special_bestWeight value = 10 }
							set_variable = { which = cgm_special_bestBuilding value = -2080374.301 }
						}
						save_event_target_as = cgm_best_planet_for_special
					}
					if = {
						limit = {
							OR = { has_building_construction = yes has_building = yes }
						}
						owner = { set_country_flag = cgm_auto_built }
						else = {
							if = {
								limit = {
									owner = { has_non_swapped_tradition = tr_diplomacy_alien_tourism }
									NOR = { has_building = building_visitor_center }
									owner = {
										check_variable = { which = cgm_special_bestWeight value < 10 }
									}
								}
								prev = {
									set_variable = { which = cgm_special_bestWeight value = 10 }
									set_variable = { which = cgm_special_bestBuilding value = -2046819.869 }
								}
								save_event_target_as = cgm_best_planet_for_special
							}
							if = {
								limit = {
									OR = { has_building_construction = yes has_building = yes }
								}
								owner = { set_country_flag = cgm_auto_built }
								else = {
									if = {
										limit = {
											owner = { has_technology = "tech_cultural_heritage" }
											owner = {
												OR = { has_ethic = ethic_spiritualist has_ethic = ethic_fanatic_spiritualist }
											}
											NOT = { is_planet_class = pc_habitat }
											NOR = {
												has_building = building_autochthon_monument
												has_building = building_heritage_site
												has_building = building_hypercomms_forum
												has_building = building_autocurating_vault
											}
											NOR = {
												has_building = building_temple
												has_building = building_holotemple
												has_building = building_sacred_nexus
												has_building = building_citadel_of_faith
											}
											owner = {
												check_variable = { which = cgm_special_bestWeight value < 10 }
											}
										}
										prev = {
											set_variable = { which = cgm_special_bestWeight value = 10 }
											set_variable = { which = cgm_special_bestBuilding value = -2013265.437 }
										}
										save_event_target_as = cgm_best_planet_for_special
									}
								}
							}
						}
					}
				}
			}
		}
	}
}
add_minerals_building_co = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
		else = {
			add_building_construction = building_thermal_borehole
			if = {
				limit = {
					OR = { has_building_construction = yes has_building = yes }
				}
				owner = { set_country_flag = cgm_auto_built }
				else = {
					add_building_construction = building_biomaterials_plant
					if = {
						limit = {
							OR = { has_building_construction = yes has_building = yes }
						}
						owner = { set_country_flag = cgm_auto_built }
						else = { add_building_construction = building_slave_processing }
					}
				}
			}
		}
	}
}
add_food_building_co = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
		else = {
			add_building_construction = building_paradise_dome
			if = {
				limit = {
					OR = { has_building_construction = yes has_building = yes }
				}
				owner = { set_country_flag = cgm_auto_built }
				else = { add_building_construction = building_slave_processing }
			}
		}
	}
}
add_society_research_building_co = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
		else = {
			add_building_construction = building_military_academy
			if = {
				limit = {
					OR = { has_building_construction = yes has_building = yes }
				}
				owner = { set_country_flag = cgm_auto_built }
				else = { add_building_construction = building_clinic }
			}
		}
	}
}
add_engineering_research_building_co = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
		else = { add_building_construction = building_military_academy }
	}
}
add_energy_building_co = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
		else = {
			add_building_construction = building_thermal_borehole
			if = {
				limit = {
					OR = { has_building_construction = yes has_building = yes }
				}
				owner = { set_country_flag = cgm_auto_built }
				else = {
					add_building_construction = building_biofuel_plant
					if = {
						limit = {
							OR = { has_building_construction = yes has_building = yes }
						}
						owner = { set_country_flag = cgm_auto_built }
						else = { add_building_construction = building_visitor_center }
					}
				}
			}
		}
	}
}
add_food_adjacency_building_co = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
		else = {
			add_building_construction = building_food_bazaar
			if = {
				limit = {
					OR = { has_building_construction = yes has_building = yes }
				}
				owner = { set_country_flag = cgm_auto_built }
				else = {
					add_building_construction = building_biomaterials_plant
					if = {
						limit = {
							OR = { has_building_construction = yes has_building = yes }
						}
						owner = { set_country_flag = cgm_auto_built }
						else = {
							add_building_construction = building_biofuel_plant
							if = {
								limit = {
									OR = { has_building_construction = yes has_building = yes }
								}
								owner = { set_country_flag = cgm_auto_built }
								else = { add_building_construction = building_soil_enrich }
							}
						}
					}
				}
			}
		}
	}
}
add_society_research_adjacency_building_co = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
		else = {
			add_building_construction = building_research_coordination
			if = {
				limit = {
					OR = { has_building_construction = yes has_building = yes }
				}
				owner = { set_country_flag = cgm_auto_built }
				else = {
					add_building_construction = building_biomaterials_plant
					if = {
						limit = {
							OR = { has_building_construction = yes has_building = yes }
						}
						owner = { set_country_flag = cgm_auto_built }
						else = { add_building_construction = building_biofuel_plant }
					}
				}
			}
		}
	}
}
add_engineering_research_adjacency_building_co = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
		else = {
			add_building_construction = building_research_coordination
			if = {
				limit = {
					OR = { has_building_construction = yes has_building = yes }
				}
				owner = { set_country_flag = cgm_auto_built }
				else = { add_building_construction = building_thermal_borehole }
			}
		}
	}
}
add_physics_research_adjacency_building_co = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
		else = {
			add_building_construction = building_comm_hub
			if = {
				limit = {
					OR = { has_building_construction = yes has_building = yes }
				}
				owner = { set_country_flag = cgm_auto_built }
				else = {
					add_building_construction = building_deep_space_scanner
					if = {
						limit = {
							OR = { has_building_construction = yes has_building = yes }
						}
						owner = { set_country_flag = cgm_auto_built }
						else = { add_building_construction = building_research_coordination }
					}
				}
			}
		}
	}
}
add_physics_research_building_co = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
		else = { add_building_construction = building_deep_space_scanner }
	}
}
add_special_resource_building_co = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
		else = { add_building_construction = building_resort }
	}
}
