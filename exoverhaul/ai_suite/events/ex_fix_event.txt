namespace = ex_fix_event

event = {
	id = ex_fix_event.1
	is_triggered_only = yes
	hide_window = yes
	
	immediate = {
		every_country = {
			limit = { is_country_type = default	} 
			run_ai_strategic_data = yes
		}
	}
}	