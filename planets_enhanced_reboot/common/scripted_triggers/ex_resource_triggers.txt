### you can insert any of the triggers below (any other of course) into any of the conditions below, as you see fit.
#######
#is_modifier_enabled = no
	#is_unique_sr_enabled = no
	#is_orbital_sr_enabled = no
	#is_planetary_sr_enabled = no	
	#is_ringworld = no	
	#has_owner = no
	#is_colony = no
	#is_colonizable = no
	#has_pop = no
	#has_building = no
	#is_terraformed = no
	#starting_system = no
	#solar_system = { #use this check to find unique systems, e.g. leviathan systems, custom homeworlds etc. 
		#OR/AND/NOT/NOR/NAND/IF = {
			#has_star_flag = xyz_flag_name_1
			#has_star_flag = xyz_flag_name_2
			#has_star_flag = xyz_flag_name_3
			#etc.
		}
	}
	#is_homeworld = no
	#is_capital = no
	
#### To restrict to particular planet types use "=". each check is for a single planet class using the OR field below!	
#OR = { 
		#check_variable = { which = "planet_group_id" value = 0.1 } -
		#check_variable = { which = "planet_group_id" value = 1.1 } - if you use "=" you are restricting this to a particular planet type
		#check_variable = { which = "planet_group_id" value = 1.2 } - if you use "=" you are restricting this to a particular planet type
		#check_variable = { which = "planet_group_id" value = 1.3 } - if you use "=" you are restricting this to a particular planet type		
	}
}	

#### To restrict to a particular planet group you need to check a number range. You require therefore
#### two checks(!). one check checking value > x and the next check value < y
#AND = {
	#check_variable = { which = "planet_group_id" value > 1 } #if you use "><" you need two checks, and you are limiting to a planet range
	#check_variable = { which = "planet_group_id" value < 2 } #if you use "><" you need two checks, as here, and you are limiting to a planet range
#}

#### To restrict to both particular planets and a specific group, use a combined OR+AND like this:
#OR = { 
		#check_variable = { which = "planet_group_id" value = 0.1 } -
		#check_variable = { which = "planet_group_id" value = 1.1 } - if you use "=" you are restricting this to a particular planet type
		#check_variable = { which = "planet_group_id" value = 1.2 } - if you use "=" you are restricting this to a particular planet type
		#check_variable = { which = "planet_group_id" value = 1.3 } - if you use "=" you are restricting this to a particular planet type		
		#AND = {
			#check_variable = { which = "planet_group_id" value > 1 } #if you use "><" you need two checks, and you are limiting to a planet range
			#check_variable = { which = "planet_group_id" value < 2 } #if you use "><" you need two checks, as here, and you are limiting to a planet range
		#}	
	}
}	

####	
####### replicate the fields below as necessary and fill them with your conditions
## unique resource conditions - please make sure the change the <custom_unique_resource_1/2/3...> to your unique resource name
conditions_sr_ex_unique_1 = {
	is_unique_sr_enabled = yes
	has_owner = no
	is_colony = no
	is_colonizable = yes
	is_homeworld = no
	is_capital = no
	has_pop = no
	has_building = no
	is_terraformed = no
	starting_system = no
	solar_system = { #use this check to find unique systems, e.g. leviathan systems, custom homeworlds etc. 
		NOR = {
			has_star_flag = empire_home_system
			has_star_flag = sol_system
			has_star_flag = deneb_system		
		}
	}	
}