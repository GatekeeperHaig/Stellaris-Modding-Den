 # This file was created by script!
 # Instead of editing it, you should change the python script.
 # Changes to the file will be overwritten the next time the script is run.
add_special_resource_building_eac = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = {
		add_building_construction = eac_building_alchemists_forge
		if = {
			limit = {
				OR = { has_building_construction = yes has_building = yes }
			}
			owner = { set_country_flag = cgm_auto_built }
		}
		else = {
			add_building_construction = eac_building_anomaly_research_facility
			if = {
				limit = {
					OR = { has_building_construction = yes has_building = yes }
				}
				owner = { set_country_flag = cgm_auto_built }
			}
			else = {
				add_building_construction = eac_building_green_smoothie_factory
				if = {
					limit = {
						OR = { has_building_construction = yes has_building = yes }
					}
					owner = { set_country_flag = cgm_auto_built }
				}
				else = {
					add_building_construction = eac_building_bio_lubricant_refinery
					if = {
						limit = {
							OR = { has_building_construction = yes has_building = yes }
						}
						owner = { set_country_flag = cgm_auto_built }
					}
					else = {
						add_building_construction = eac_building_rare_minerals_mine
						if = {
							limit = {
								OR = { has_building_construction = yes has_building = yes }
							}
							owner = { set_country_flag = cgm_auto_built }
						}
						else = { add_building_construction = eac_building_fertile_land_farm }
					}
				}
			}
		}
	}
}
add_food_building_eac = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = {
		add_building_construction = eac_building_food_packaging_plant_1
		if = {
			limit = {
				OR = { has_building_construction = yes has_building = yes }
			}
			owner = { set_country_flag = cgm_auto_built }
		}
		else = {
			add_building_construction = eac_building_gourmet_restaurant
			if = {
				limit = {
					OR = { has_building_construction = yes has_building = yes }
				}
				owner = { set_country_flag = cgm_auto_built }
			}
			else = { add_building_construction = eac_building_paste_processing_plant }
		}
	}
}
add_minerals_building_eac = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = { add_building_construction = eac_building_luxury_goods_factory }
}
add_physics_research_building_eac = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = {
		add_building_construction = eac_building_grav_application_lab
		if = {
			limit = {
				OR = { has_building_construction = yes has_building = yes }
			}
			owner = { set_country_flag = cgm_auto_built }
		}
		else = { add_building_construction = eac_building_hab_laboratory_physics_module }
	}
}
add_engineering_research_building_eac = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = {
		add_building_construction = eac_building_advanced_prefab_facility
		if = {
			limit = {
				OR = { has_building_construction = yes has_building = yes }
			}
			owner = { set_country_flag = cgm_auto_built }
		}
		else = { add_building_construction = eac_building_hab_laboratory_engineering_module }
	}
}
add_society_research_building_eac = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = { add_building_construction = eac_building_hab_laboratory_bio_module }
}
