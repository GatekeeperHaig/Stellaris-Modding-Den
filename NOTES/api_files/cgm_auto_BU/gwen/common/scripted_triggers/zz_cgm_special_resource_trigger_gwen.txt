 # This file was created by script!
 # Instead of editing it, you should change the python script.
 # Changes to the file will be overwritten the next time the script is run.
special_resource_any_building_available_gwen = {
	OR = {
		AND = {
			has_resource = { type = sr_h2o amount > 0 }
			owner = { has_technology = "tech_gwen_aquatic_species_advanced_water_purification" }
			NOT = { owner = { has_authority = auth_machine_intelligence } }
			planet = { NOT = { is_planet_class = pc_ocean } }
			AND = {
				has_resource = { type = sr_h2o amount > 0 }
			}
		}
	}
}
