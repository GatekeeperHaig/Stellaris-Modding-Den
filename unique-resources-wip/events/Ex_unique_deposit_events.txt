namespace = ex_unique_deposit_events

@unique_spawn_chance = 100

event = {
	id = ex_unique_deposit_events.1
	hide_window = yes
	is_triggered_only = yes
	fire_only_once = yes
	
	immediate = {
		if = {
			limit = { NOT = { has_event_flag = unique_1_fired } }
			random = {
				chance = @unique_spawn_chance
				random_system = {
					limit = { 
						any_planet = { 
							AND = {
								is_colonizable = yes
								planet_size > 15
								has_owner = no	
							}						
						}
					}
					random_system_planet = {
						limit = { 
							AND = {
								is_colonizable = yes
								planet_size > 15
								has_owner = no	
							}	
						}
						random_tile = {
							limit = { 
								has_blocker = no 
								has_deposit = no
								has_building = no		
							}
							add_deposit = "d_sr_ex_unique_1_deposit"
						}
					}
				}
			}
			add_event_flag = unique_1_fired
		}	
		if = {
			limit = { NOT = { has_event_flag = unique_2_fired } }
			random = {
				chance = @unique_spawn_chance
				random_system = {
					limit = { 
						any_planet = { 
							AND = {
								is_colonizable = yes
								planet_size > 15
								has_owner = no	
							}						
						}
					}
					random_system_planet = {
						limit = { 
							AND = {
								is_colonizable = yes
								planet_size > 15
								has_owner = no	
							}	
						}
						random_tile = {
							limit = { 
								has_blocker = no 
								has_deposit = no
								has_building = no		
							}
							add_deposit = "d_sr_ex_unique_2_deposit"
						}
					}
				}
			}
			add_event_flag = unique_2_fired
		}
		if = {
			limit = { NOT = { has_event_flag = unique_3_fired } }
			random = {
				chance = @unique_spawn_chance
				random_system = {
					limit = { 
						any_planet = { 
							AND = {
								is_colonizable = yes
								planet_size > 15
								has_owner = no	
							}						
						}
					}
					random_system_planet = {
						limit = { 
							AND = {
								is_colonizable = yes
								planet_size > 15
								has_owner = no	
							}	
						}
						random_tile = {
							limit = { 
								has_blocker = no 
								has_deposit = no
								has_building = no		
							}
							add_deposit = "d_sr_ex_unique_3_deposit"
						}
					}
				}
			}
			add_event_flag = unique_3_fired
		}	
		if = {
			limit = { NOT = { has_event_flag = unique_4_fired } }
			random = {
				chance = @unique_spawn_chance
				random_system = {
					limit = { 
						any_planet = { 
							AND = {
								is_colonizable = yes
								planet_size > 15
								has_owner = no	
							}						
						}
					}
					random_system_planet = {
						limit = { 
							AND = {
								is_colonizable = yes
								planet_size > 15
								has_owner = no	
							}	
						}
						random_tile = {
							limit = { 
								has_blocker = no 
								has_deposit = no
								has_building = no		
							}
							add_deposit = "d_sr_ex_unique_4_deposit"
						}
					}
				}
			}
			add_event_flag = unique_4_fired
		}	
		if = {
			limit = { NOT = { has_event_flag = unique_5_fired } }
			random = {
				chance = @unique_spawn_chance
				random_system = {
					limit = { 
						any_planet = { 
							AND = {
								is_colonizable = yes
								planet_size > 15
								has_owner = no	
							}						
						}
					}
					random_system_planet = {
						limit = { 
							AND = {
								is_colonizable = yes
								planet_size > 15
								has_owner = no	
							}	
						}
						random_tile = {
							limit = { 
								has_blocker = no 
								has_deposit = no
								has_building = no		
							}
							add_deposit = "d_sr_ex_unique_5_deposit"
						}
					}
				}
			}
			add_event_flag = unique_5_fired
		}	
		if = {
			limit = { NOT = { has_event_flag = unique_6_fired } }
			random = {
				chance = @unique_spawn_chance
				random_system = {
					limit = { 
						any_planet = { 
							AND = {
								is_colonizable = yes
								planet_size > 15
								has_owner = no	
							}						
						}
					}
					random_system_planet = {
						limit = { 
							AND = {
								is_colonizable = yes
								planet_size > 15
								has_owner = no	
							}	
						}
						random_tile = {
							limit = { 
								has_blocker = no 
								has_deposit = no
								has_building = no		
							}
							add_deposit = "d_sr_ex_unique_6_deposit"
						}
					}
				}
			}
			add_event_flag = unique_6_fired
		}
		
	}
}	