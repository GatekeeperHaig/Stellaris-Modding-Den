 # This file was created by script!
 # Instead of editing it, you should change the python script.
 # Changes to the file will be overwritten the next time the script is run.
add_minerals_adjacency_building_tycoon = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = { add_building_construction = building_industrial_complex_1 }
}
add_special_resource_building_tycoon = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = {
		add_building_construction = building_entertainment_center
		if = {
			limit = {
				OR = { has_building_construction = yes has_building = yes }
			}
			owner = { set_country_flag = cgm_auto_built }
		}
		else = { add_building_construction = Garanthium_betharian_power_plant_1 }
	}
}
add_energy_building_tycoon = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = { add_building_construction = darkmatter_power_plant_1 }
}
add_physics_research_building_tycoon = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = { add_building_construction = darkmatter_power_plant_1 }
}
add_engineering_research_building_tycoon = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = { add_building_construction = darkmatter_power_plant_1 }
}
add_energy_adjacency_building_tycoon = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = {
		add_building_construction = building_battery_array_1
		if = {
			limit = {
				OR = { has_building_construction = yes has_building = yes }
			}
			owner = { set_country_flag = cgm_auto_built }
		}
		else = { add_building_construction = building_bank_1 }
	}
}
