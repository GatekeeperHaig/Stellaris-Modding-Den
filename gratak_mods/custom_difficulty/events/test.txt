namespace = testdiff


event = {
	id = testdiff.0
	is_triggered_only = yes
	hide_window = yes
	immediate = {	
		set_variable = { which = blabla value = 25.001 } 
	}
}

event = {
	id = testdiff.1
	is_triggered_only = yes
	title = "testdiff.1.name"
	desc = "testdiff.1.desc"
	option = {
		name = REMARKABLE
	}	
	option = {
		name = increase_test.25
		hidden_effect = {
			change_variable = { which = blabla value = 25 } 
			country_event = { id = testdiff.1 }
		}
	}
	option = {
		name = increase_test.25
		hidden_effect = {
			change_variable = { which = blabla value = 25 } 
			country_event = { id = testdiff.1 }
		}
	}
	option = {
		name = increase_test.25
		hidden_effect = {
			change_variable = { which = blabla value = 25 } 
			country_event = { id = testdiff.1 }
		}
	}
	option = {
		name = increase_test.25
		hidden_effect = {
			change_variable = { which = blabla value = 25 } 
			country_event = { id = testdiff.1 }
		}
	}
	option = {
		name = increase_test.25
		hidden_effect = {
			change_variable = { which = blabla value = 25 } 
			country_event = { id = testdiff.1 }
		}
	}
	option = {
		name = decrease_test.25
		hidden_effect = {
			change_variable = { which = blabla value = -25 } 
			country_event = { id = testdiff.1 }
		}
	}
	option = {
		name = decrease_test.25
		hidden_effect = {
			change_variable = { which = blabla value = -25 } 
			country_event = { id = testdiff.1 }
		}
	}
	option = {
		name = decrease_test.25
		hidden_effect = {
			change_variable = { which = blabla value = -25 } 
			country_event = { id = testdiff.1 }
		}
	}
	option = {
		name = decrease_test.25
		hidden_effect = {
			change_variable = { which = blabla value = -25 } 
			country_event = { id = testdiff.1 }
		}
	}
	option = {
		name = decrease_test.25
		hidden_effect = {
			change_variable = { which = blabla value = -25 } 
			country_event = { id = testdiff.1 }
		}
	}
	option = {
		trigger = {
			check_variable = {
				which = blabla
				value = 0.001
			}
		}
		name = OK
	}
	option = {
		trigger = {
			check_variable = {
				which = blabla
				value > 0
			}
		}
		name = blub
	}
}