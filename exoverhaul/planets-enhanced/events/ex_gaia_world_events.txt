namespace = ex_gaia_world_events

planet_event = {
	id = ex_gaia_world_events.1
	hide_window = yes
	
	trigger = {
		root = {
			is_planet_class = pc_gaia
			NOT = { has_modifier = ex_gaia_world }		
		}	
	}	
	
	immediate = {
		root = { add_modifier = { modifier = "ex_gaia_world" days = -1 } }												
	}
}