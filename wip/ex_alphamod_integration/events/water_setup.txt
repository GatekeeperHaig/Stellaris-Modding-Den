namespace = water_setup

event = {
	id = water_setup.1

	is_triggered_only = yes
	hide_window = yes
	
	immediate = {
		every_country = {
			limit = {
				or = {
					has_civic = civic_idealistic_foundation
					has_civic = civic_environmentalist
					has_trait = "trait_conservational"
				}
			}
			give_technology = { message = no tech = tech_alpha_adv_renewable }
		}			
	}
}