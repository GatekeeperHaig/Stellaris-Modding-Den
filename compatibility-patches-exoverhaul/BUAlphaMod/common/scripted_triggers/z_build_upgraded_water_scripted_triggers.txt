#This file was created by script!
#Instead of editing it, you should change the origin files or the script and rerun the script!
#Python files that can be directly used for a rerun (storing all parameters from the last run) should be in the main directory
has_food_deposit = {
	
	has_resource = {
		type = food
		amount > 0
	}
}

has_water_deposit = {
	
	has_resource = {
		type = sr_h2o
		amount > 0
	}
}

has_minerals_deposit = {
	
	has_resource = {
		type = minerals
		amount > 0
	}
}

has_energy_deposit = {
	
	has_resource = {
		type = energy
		amount > 0
	}
}

has_ice_deposit = {
	
	has_resource = {
		type = sr_ice
		amount > 0
	}
}

has_water_hub = {
	
	OR = {
		has_building = "building_water_excavator"
		has_building_water_excavator_2 = yes
		has_building = "building_reservoir"
		has_building_reservoir_2 = yes
		has_building_reservoir_3 = yes
		has_building = "building_hydro_plant"
		has_building_hydro_plant_2 = yes
		has_building = "building_ice_processor"
		has_building = "building_wet_farm"
		has_building = "building_fishing_yard"
	}
}

has_water_source = {
	
	OR = {
		has_water_hub = yes
		any_tile = {
			OR = {
				has_water_deposit = yes
				has_ice_deposit = yes
			}
		}
	}
}

is_hab_or_ringyard = {
	is_planet_class = pc_habitat
}
