 # This file was created by script!
 # Instead of editing it, you should change the python script.
 # Changes to the file will be overwritten the next time the script is run.
add_energy_building_expstr = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = {
		add_building_construction = building_kugelblitz_tower
		if = {
			limit = {
				OR = { has_building_construction = yes has_building = yes }
			}
			owner = { set_country_flag = cgm_auto_built }
		}
		else = {
			add_building_construction = building_virtual_capital
			if = {
				limit = {
					OR = { has_building_construction = yes has_building = yes }
				}
				owner = { set_country_flag = cgm_auto_built }
			}
			else = {
				add_building_construction = building_virtual_bank
				if = {
					limit = {
						OR = { has_building_construction = yes has_building = yes }
					}
					owner = { set_country_flag = cgm_auto_built }
				}
				else = {
					add_building_construction = building_virtual_city
					if = {
						limit = {
							OR = { has_building_construction = yes has_building = yes }
						}
						owner = { set_country_flag = cgm_auto_built }
					}
					else = {
						add_building_construction = building_pure_purplarium_power_plant
						if = {
							limit = {
								OR = { has_building_construction = yes has_building = yes }
							}
							owner = { set_country_flag = cgm_auto_built }
						}
						else = { add_building_construction = building_iodizium_plant }
					}
				}
			}
		}
	}
}
cgm_add_special_building_expstr = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = {
		if = {
			limit = {
				owner = {
					check_variable = { which = cgm_special_bestBuilding value = -2092317.947 }
				}
			}
			add_building_construction = building_outside_connection
		}
		if = {
			limit = {
				OR = { has_building_construction = yes has_building = yes }
			}
			owner = { set_country_flag = cgm_auto_built }
		}
		else = {
			if = {
				limit = {
					owner = {
						check_variable = { which = cgm_special_bestBuilding value = -2058763.515 }
					}
				}
				add_building_construction = building_super_adventure_building
			}
			if = {
				limit = {
					OR = { has_building_construction = yes has_building = yes }
				}
				owner = { set_country_flag = cgm_auto_built }
			}
			else = {
				if = {
					limit = {
						owner = {
							check_variable = { which = cgm_special_bestBuilding value = -2025209.083 }
						}
					}
					add_building_construction = building_virtual_city
				}
			}
		}
	}
}
cgm_search_for_special_building_expstr = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = {
		if = {
			limit = {
				is_planet_class = pc_virtual_world
				custom_tooltip = {
					text = "requires_building_virtual_capital"
					OR = { has_building = "building_virtual_capital" }
				}
				NOR = { has_building = building_outside_connection }
				owner = {
					check_variable = { which = cgm_special_bestWeight value < 10 }
				}
			}
			prev = {
				set_variable = { which = cgm_special_bestWeight value = 10 }
				set_variable = { which = cgm_special_bestBuilding value = -2092317.947 }
			}
			save_event_target_as = cgm_best_planet_for_special
		}
		if = {
			limit = {
				OR = { has_building_construction = yes has_building = yes }
			}
			owner = { set_country_flag = cgm_auto_built }
		}
		else = {
			if = {
				limit = {
					is_planet_class = pc_virtual_world
					custom_tooltip = {
						text = "requires_building_virtual_capital"
						OR = { has_building = "building_virtual_capital" }
					}
					NOR = { has_building = building_super_adventure_building }
					owner = {
						check_variable = { which = cgm_special_bestWeight value < 10 }
					}
				}
				prev = {
					set_variable = { which = cgm_special_bestWeight value = 10 }
					set_variable = { which = cgm_special_bestBuilding value = -2058763.515 }
				}
				save_event_target_as = cgm_best_planet_for_special
			}
			if = {
				limit = {
					OR = { has_building_construction = yes has_building = yes }
				}
				owner = { set_country_flag = cgm_auto_built }
			}
			else = {
				if = {
					limit = {
						is_planet_class = pc_virtual_world
						custom_tooltip = {
							text = "requires_building_virtual_capital"
							OR = { has_building = "building_virtual_capital" }
						}
						owner = {
							check_variable = { which = cgm_special_bestWeight value < 10 }
						}
					}
					prev = {
						set_variable = { which = cgm_special_bestWeight value = 10 }
						set_variable = { which = cgm_special_bestBuilding value = -2025209.083 }
					}
					save_event_target_as = cgm_best_planet_for_special
				}
			}
		}
	}
}
add_society_research_building_expstr = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = {
		add_building_construction = building_iodizium_research
		if = {
			limit = {
				OR = { has_building_construction = yes has_building = yes }
			}
			owner = { set_country_flag = cgm_auto_built }
		}
		else = { add_building_construction = building_virtual_science_lab }
	}
}
add_physics_research_building_expstr = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = {
		add_building_construction = building_iodizium_research
		if = {
			limit = {
				OR = { has_building_construction = yes has_building = yes }
			}
			owner = { set_country_flag = cgm_auto_built }
		}
		else = { add_building_construction = building_virtual_science_lab }
	}
}
add_engineering_research_building_expstr = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = {
		add_building_construction = building_iodizium_research
		if = {
			limit = {
				OR = { has_building_construction = yes has_building = yes }
			}
			owner = { set_country_flag = cgm_auto_built }
		}
		else = { add_building_construction = building_virtual_science_lab }
	}
}
add_energy_adjacency_building_expstr = {
	if = {
		limit = {
			OR = { has_building_construction = yes has_building = yes }
		}
		owner = { set_country_flag = cgm_auto_built }
	}
	else = { add_building_construction = building_kugelblitz_tower }
}
