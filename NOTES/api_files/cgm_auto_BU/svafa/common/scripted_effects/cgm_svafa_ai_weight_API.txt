check_pop_traits_svafa = {
	if = {
		limit = { is_robot_pop = no }
		if = {
			limit = { has_trait = trait_svafa_lithovore }
			prev = {
				change_variable = { which = minerals_mult_planet_pop value = -0.1 }
			}
		}
		if = {
			limit = { has_trait = trait_svafa_carnivorous }
			prev = {
				change_variable = { which = food_mult_planet_pop value = -0.1 }
			}
		}
		if = {
			limit = { has_trait = trait_svafa_autotrophic }
			prev = {
				change_variable = { which = food_mult_planet_pop value = 0.1 }
			}
		}
		if = {
			limit = { has_trait = trait_svafa_electrovore }
			prev = {
				change_variable = { which = energy_mult_planet_pop value = -0.1 }
			}
		}
		if = {
			limit = { has_trait = trait_svafa_bioelectrogenesis }
			prev = {
				change_variable = { which = energy_mult_planet_pop value = 0.1 }
			}
		}
	}
	if = {
		limit = { is_robot_pop = yes }
		if = {
			limit = { has_trait = trait_svafa_lithovore_machine }
			prev = {
				change_variable = { which = minerals_mult_planet_pop value = -0.1 }
			}
		}
		if = {
			limit = { has_trait = trait_svafa_autotrophic_machine }
			prev = {
				change_variable = { which = energy_mult_planet_pop value = 0.1 }
			}
		}
		if = {
			limit = { has_trait = trait_svafa_electrovore_machine }
			prev = {
				change_variable = { which = energy_mult_planet_pop value = -0.1 }
			}
		}
	}
	if = {
		limit = { has_trait = trait_svafa_multidextrous }
		prev = {
			change_variable = { which = food_mult_planet_pop value = 0.05 }
			change_variable = { which = energy_mult_planet_pop value = 0.05 }
			change_variable = { which = minerals_mult_planet_pop value = 0.05 }
		}
	}
	if = {
		limit = { has_trait = trait_svafa_electrogenic_defense }
		prev = {
			change_variable = { which = energy_mult_planet_pop value = 0.05 }
		}
	}
	if = {
		limit = { has_trait = trait_svafa_eidetic_memory }
		prev = {
			change_variable = { which = engineering_research_mult_planet_pop value = 0.2 }
			change_variable = { which = physics_research_mult_planet_pop value = 0.2 }
			change_variable = { which = society_research_mult_planet_pop value = 0.2 }
		}
	}
}
