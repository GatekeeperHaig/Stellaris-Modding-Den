uninhabitable_planets_spawn_table = {	
	
	if = {
		limit = { is_moon = yes }
		random_list = {
			20 = {
				change_pc = pc_orbiting_asteroid
				new_worlds_adjust_asteroids_size = yes
				modifier = { factor = 0 is_gas_giant_moon = yes }
			}
			80 = {
				 #this means nothing will happen - planet will remain the same
			}
		}
	}
}
