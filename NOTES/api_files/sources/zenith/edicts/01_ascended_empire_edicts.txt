###ZoFE Edicts



country_edict = {
	name = "make_xeno_preserve"

	cost = { influence = 300 }

	length = 0

	potential = {
		has_valid_civic = civic_xenophile_fervor
		NOT = { has_country_flag = search_for_preserve_world }
	}

	allow = {
		custom_tooltip = {
			fail_text = has_preserve_world_planet
			NOT = { has_modifier = has_preserve_world_planet }
		}
		custom_tooltip = {
			fail_text = has_preserve_world_candidate
			any_planet_within_border = {
				is_planet_class = pc_gaia
				planet_size > 20
				num_species > 8
				free_building_tiles > 0
			}
		}
	}


	effect = {
		custom_tooltip = make_preserve_world_effect
		hidden_effect = { 
			set_country_flag = search_for_preserve_world
			country_event = { id = zofe.1 } 
		}
	}

	ai_weight = {
		weight = 0
	}
}
