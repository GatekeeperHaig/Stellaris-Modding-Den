namespace = nw_flag

country_event = {
	id = nw_flag.1
	hide_window = yes
	fire_only_once = yes
	
	trigger = { NOT = { has_global_flag = new_worlds_active } }
	
	immediate = { set_global_flag = new_worlds_active }
}