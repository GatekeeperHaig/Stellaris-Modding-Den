namespace = henrik

planet_event = {
	id = henrik.239
	hide_window = yes
	
	trigger = {
		always = no
	}
	
	immediate = {
		destroy_fleet = spaceport
		create_spaceport = {
			owner = root
			initial_module = "defensive_spaceport_explosives"
		}
		spaceport = {
			set_spaceport_level = 6
		}
	}
}