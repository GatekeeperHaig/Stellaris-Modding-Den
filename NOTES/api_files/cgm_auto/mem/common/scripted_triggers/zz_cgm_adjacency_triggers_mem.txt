 # This file was created by script!
 # Instead of editing it, you should change the python script.
 # Changes to the file will be overwritten the next time the script is run.
 #this = planet
 #prev = tile
engineering_research_adjacency_any_building_available_mem = {
	OR = {
		AND = {
			prev = {
				planet = {
					has_planet_flag = building_mem_matrix_building
					NOT = { has_building = building_mem_matrix_building }
				}
			}
			prev = {
				planet = {
					has_planet_flag = building_mem_matrix_building
					NOT = { has_building = building_mem_matrix_building }
				}
			}
			NOT = {
				owner = {
					any_owned_planet = { OR = { has_building = building_mem_matrix_building_replace } }
				}
			}
		}
		AND = {
			prev = { planet = { has_planet_flag = mem_ancient_robot_building } }
			prev = { planet = { has_planet_flag = mem_ancient_robot_building } }
			NOT = {
				owner = {
					any_owned_planet = { OR = { has_building = building_mem_ancient_robot_building_2 } }
				}
			}
		}
	}
}
 #this = planet
 #prev = tile
society_research_adjacency_any_building_available_mem = {
	OR = {
		AND = {
			prev = {
				planet = {
					has_planet_flag = building_mem_matrix_building
					NOT = { has_building = building_mem_matrix_building }
				}
			}
			prev = {
				planet = {
					has_planet_flag = building_mem_matrix_building
					NOT = { has_building = building_mem_matrix_building }
				}
			}
			NOT = {
				owner = {
					any_owned_planet = { OR = { has_building = building_mem_matrix_building_replace } }
				}
			}
		}
	}
}
 #this = planet
 #prev = tile
physics_research_adjacency_any_building_available_mem = {
	OR = {
		AND = {
			prev = {
				planet = {
					has_planet_flag = building_mem_matrix_building
					NOT = { has_building = building_mem_matrix_building }
				}
			}
			prev = {
				planet = {
					has_planet_flag = building_mem_matrix_building
					NOT = { has_building = building_mem_matrix_building }
				}
			}
			NOT = {
				owner = {
					any_owned_planet = { OR = { has_building = building_mem_matrix_building_replace } }
				}
			}
		}
	}
}
