namespace = relocation

planet_event = {
	id = relocation.1
	is_triggered_only = yes
	hide_window = yes
	
	immediate = {
		planet_event = {
			id = relocation.2
		}
	}
}

planet_event = {
	id = relocation.2
	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_pop = {
			species = owner_main_species
			ethos = owner
		}
		create_pop = {
			species = owner_main_species
			ethos = owner
		}
	}
}