# Colour planet name based on requirements and give it an icon
c_colour_planet_name = {
	if = { limit = {
			AND = {
				has_planet_flag = pm_is_precursor_world
				NOT = { has_planet_flag = pm_is_precursor_world_colour }
		} 
		set_name = color_precursor
		set_planet_flag = pm_is_precursor_world_colour
		log = ***- Planet given a blue colour and icon----
		log = ***-------- Planet name: [This.GetName] ----

	}
	if = { limit = {
			AND = {
				has_planet_flag = pm_is_wondrous_world
				NOT = { has_planet_flag = pm_is_wondrous_world_colour }
		} 
		set_name = color_planet_wonder
		set_planet_flag = pm_is_wondrous_world_colour
		log = ***- Planet given an orange colour and icon----
		log = ***-------- Planet name: [This.GetName] ----

	}
}

# Remove planet name and icon and reset name to something that makes sense
c_reset_planet_name = {
	if = { 
		limit = {
			OR = {
				has_planet_flag = pm_is_precursor_world
				has_planet_flag = pm_is_wondrous_world
				has_planet_flag = pm_is_infested_world
			}	
		}
		set_name = random
		set_name = color_default_white
		if = {	
			limit = { has_planet_flag = pm_is_precursor_world }
			# remove_planet_flag = pm_is_precursor_world   #never remove because these are always kept
			remove_planet_flag = pm_is_precursor_world_colour
		}
		if = {	
			limit = { has_planet_flag = pm_is_wondrous_world }
			remove_planet_flag = pm_is_wondrous_world
			remove_planet_flag = pm_is_wondrous_world_colour
		}
		if = {	
			limit = { has_planet_flag = pm_is_infested_world }
			remove_planet_flag = pm_is_infested_world
		}
	}	
}

# Changes planet graphics depending on modifiers
c_planet_graphics = {
	if = { 
		limit = {
			has_modifier = Precursor_Energy_Grid
			is_planet_class = pc_continental
		}
		set_planet_entity = { entity = pm_continental_planet_energy_grid_entity	}
	}
	if = { 
		limit = {
			has_modifier = Precursor_Energy_Grid
			is_planet_class = pc_gaia
		}
		set_planet_entity = { entity = pm_gaia_planet_energy_grid_entity }
	}
	if = { 
		limit = {
			has_modifier = Precursor_Energy_Grid
			is_planet_class = pc_arctic
		}
		set_planet_entity = { entity = pm_arctic_planet_energy_grid_entity }
	}
	if = { 
		limit = {
			has_modifier = Precursor_Energy_Grid
			is_planet_class = pc_alpine
		}
		set_planet_entity = { entity = pm_alpine_planet_energy_grid_entity }
	}
	if = { 
		limit = {
			has_modifier = Precursor_Energy_Grid
			is_planet_class = pc_savannah
		}
		set_planet_entity = { entity = pm_savannah_planet_energy_grid_entity }
	}
	if = { 
		limit = {
			has_modifier = Precursor_Energy_Grid
			is_planet_class = pc_arid
		}
		set_planet_entity = { entity = pm_arid_planet_energy_grid_entity }
	}
	if = { 
		limit = {
			has_modifier = Precursor_Energy_Grid
			is_planet_class = pc_barren
		}
		set_planet_entity = { entity = pm_barren_planet_energy_grid_entity }
	}
	if = { 
		limit = {
			has_modifier = Precursor_Energy_Grid
			is_planet_class = pc_barren_cold
		}
		set_planet_entity = { entity = pm_cold_barren_planet_energy_grid_entity }
	}
	if = { 
		limit = {
			has_modifier = Precursor_Energy_Grid
			is_planet_class = pc_desert
		}
		set_planet_entity = { entity = pm_desert_planet_energy_grid_entity }
	}
	if = { 
		limit = {
			has_modifier = Precursor_Energy_Grid
			is_planet_class = pc_frozen
		}
		set_planet_entity = { entity = pm_frozen_planet_energy_grid_entity }
	}
	if = { 
		limit = {
			has_modifier = Precursor_Energy_Grid
			is_planet_class = pc_molten
		}
		set_planet_entity = { entity = pm_molten_planet_energy_grid_entity }
	}
	if = { 
		limit = {
			has_modifier = Precursor_Energy_Grid
			is_planet_class = pc_ocean
		}
		set_planet_entity = { entity = pm_ocean_planet_energy_grid_entity }
	}
	if = { 
		limit = {
			has_modifier = Precursor_Energy_Grid
			is_planet_class = pc_toxic
		}
		set_planet_entity = { entity = pm_toxic_planet_energy_grid_entity }
	}
	if = { 
		limit = {
			has_modifier = Precursor_Energy_Grid
			is_planet_class = pc_tropical
		}
		set_planet_entity = { entity = pm_tropical_planet_energy_grid_entity }
	}
	if = { 
		limit = {
			has_modifier = Precursor_Energy_Grid
			is_planet_class = pc_tundra
		}
		set_planet_entity = { entity = pm_tundra_planet_energy_grid_entity }
	}
	if = { 
		limit = {
			has_modifier = Precursor_Energy_Grid
			is_planet_class = pc_nuked
		}
		set_planet_entity = { entity = pm_nuked_planet_energy_grid_entity }
	}
}
