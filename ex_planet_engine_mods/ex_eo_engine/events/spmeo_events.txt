namespace = spm

#########################################################
##### UPGRADE STRONGHOLDS TO FORTRESSES
#########################################################

event = {
    id = spm.2
	hide_window = yes
	is_triggered_only = yes

	immediate = {
		every_country = {
			IF = {
				limit = { 
					has_country_strategic_resource = { type = sr_blackhole_access amount > 1 } 
				
				
				}
			}
		}
	}
}