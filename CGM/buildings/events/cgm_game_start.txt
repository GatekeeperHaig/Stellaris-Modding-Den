#This file was created by script!
#Instead of editing it, you should change the origin files or the script and rerun the script!
#Python files that can be directly used for a rerun (storing all parameters from the last run) should be in the main directory
namespace = cgm_game_start
event = {
	#game start
	id = cgm_game_start.0
	hide_window = yes
	fire_only_once = yes
	is_triggered_only = yes
	immediate = {
		set_timed_global_flag = { flag = cgm_game_start days = 1 }
		set_global_flag = cgm_active
		random_planet = { save_global_event_target_as = cgm_var_storage }
	}
}
