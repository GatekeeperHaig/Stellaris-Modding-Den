 # This file was created by script!
 # Instead of editing it, you should change the python script.
 # Changes to the file will be overwritten the next time the script is run.
 #this = planet
 #prev = tile
physics_research_adjacency_any_building_available_legw = {
	OR = {
		AND = {
			owner = { has_technology = "tech_timeshift" }
			prev = {
				planet = {
					OR = { has_modifier = timeshift_fast has_modifier = timeshift_slow }  #End-Or
				}
			}
			prev = {
				planet = {
					OR = { has_modifier = timeshift_fast has_modifier = timeshift_slow }  #End-Or
				}
			}
			NOR = { has_building = time_institute }
		}
	}
}
 #this = planet
 #prev = tile
minerals_adjacency_any_building_available_legw = {
	OR = {
		AND = {
			prev = { planet = { has_modifier = lava_planet } }
		}
	}
}
