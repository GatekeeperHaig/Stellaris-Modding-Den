namespace = core_game_mechanics_and_ai_base

country_event = { 
	id = core_game_mechanics_and_ai_base.0
	hide_window = yes	
	fire_only_once = yes
	trigger = { NOT = { has_global_flag = core_game_mechanics_and_ai } }
	immediate = { 
		set_global_flag = core_game_mechanics_and_ai
		if = {
			limit = {
				is_multiplayer = yes
				is_ai = no					
			}	
			set_country_flag = ex_game_host
		}		
		country_event = { id = core_game_mechanics_and_ai_base.1 }
	}
}

country_event = {
	id = core_game_mechanics_and_ai_base.1	
	is_triggered_only = yes
	title = core_game_mechanics_and_ai.main_menu_title
	desc = core_game_mechanics_and_ai.main_menu_desc
	picture = GFX_CGM_buildings_menu
	trigger = { 		
		is_ai = no
		OR = {
			is_multiplayer = no
			AND = { 
				is_multiplayer = yes				
				has_country_flag = ex_game_host
			}	
		}				
	}	
	option = {
		name = vanilla_building_stats_name
		custom_tooltip = vanilla_building_stats_tooltip
		trigger = { overhauled_building_stats_enabled = yes	}		
		hidden_effect = { remove_global_flag = overhauled_building_stats_active }			
	}
	option = {
		name = overhauled_building_stats_name
		custom_tooltip = overhauled_building_stats_tooltip
		trigger = { overhauled_building_stats_enabled = no }		
		hidden_effect = { set_global_flag = overhauled_building_stats_active }		
	}		
	after = { hidden_effect = { country_event = { id = core_game_mechanics_and_ai_base.1 } } }	
}