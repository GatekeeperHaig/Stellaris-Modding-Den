ptlanet_edict = {
  name = "display_low_tier"
  
  effect = {
      hidden_effect = {   
        set_country_flag = display_low_tier_flag                
        set_country_flag = do_no_remove_low_tier_flag #for auto_build comp patch! Make sure it never removes the flag.                 
      }
  } 
  
  potential = {     
    NOT = { has_country_flag = display_low_tier_flag }
  }
  
  ai_weight = { weight = 0 }
} 

planet_edict = {
  name = "hide_low_tier"
  
  effect = {
      hidden_effect = {   
        remove_country_flag = display_low_tier_flag
        remove_country_flag = do_no_remove_low_tier_flag                
      }
  } 
  
  potential = {     
    has_country_flag = display_low_tier_flag
  }
  
  ai_weight = { weight = 0 }
} 